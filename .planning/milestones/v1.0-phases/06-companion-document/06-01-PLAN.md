---
phase: 06-companion-document
plan: 01
type: execute
wave: 1
depends_on: []
files_modified: [companion.md]
autonomous: true
requirements: [COMP-01, COMP-02, COMP-03]

must_haves:
  truths:
    - "A reader unfamiliar with GSD can read Sections 1-3 and understand what GSD is, why it exists, and how to start using it"
    - "Section 1 explains the problem (context loss, no verification, no structure) and defines what an agent framework is (LLM + instructions + tools + memory)"
    - "Section 2 explains GSD architecture (orchestrator + 18 agents in 4 roles), .planning/ as persistent memory, and the core principle (du bestämmer, GSD strukturerar)"
    - "Section 3 covers all four on-ramp commands (/gsd-fast, /gsd-do, /gsd-quick, full workflow) with terminal examples and a decision tree"
    - "No Marp directives appear anywhere in the document"
    - "All terminology follows TERMINOLOGY.md (English technical terms, no forbidden Swedish equivalents)"
  artifacts:
    - path: "companion.md"
      provides: "Sections 1-3 with full content"
      contains: "Problemet med ostrukturerad AI-assistans"
  key_links:
    - from: "companion.md Section 1"
      to: "slides.md Sections 1-2"
      via: "expanded content — not duplicated"
      pattern: "agent framework"
    - from: "companion.md Section 3"
      to: "slides.md Section 4"
      via: "expanded on-ramp commands with more examples"
      pattern: "gsd-fast.*gsd-quick.*gsd-do"
---

<objective>
Write Sections 1-3 of the companion document, transforming the outline headers into comprehensive content that expands on the presentation slides.

Purpose: Establish the foundational knowledge — after reading these 3 sections, a developer who has never seen the presentation understands what GSD is, why it exists, and how to start using it today.

Output: companion.md with Sections 1 (Introduktion), 2 (GSD i korthet), and 3 (Kom igång — On-Ramp) filled with full content.
</objective>

<execution_context>
@~/.copilot/get-shit-done/workflows/execute-plan.md
@~/.copilot/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/06-companion-document/06-CONTEXT.md
@.planning/phases/06-companion-document/06-RESEARCH.md

@companion.md
@slides.md
@TERMINOLOGY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Write Section 1 (Introduktion) and Section 2 (GSD i korthet)</name>
  <files>companion.md</files>
  <read_first>
    - companion.md (current outline — see existing header structure to replace)
    - slides.md (lines 58-230 — Sections 1-3 of slides: problem statement, framework concept, BMAD comparison, GSD mental model, agent architecture, wave execution, .planning/ directory)
    - TERMINOLOGY.md (language policy — all English terms listed must stay English)
    - .planning/phases/06-companion-document/06-CONTEXT.md (decisions D-01 through D-14)
    - .planning/phases/06-companion-document/06-RESEARCH.md (patterns, anti-patterns, writing guidelines)
  </read_first>
  <action>
Replace the placeholder headers for Sections 1 and 2 with full content. Keep the document header and tagline. Write in Swedish prose with English technical terms per TERMINOLOGY.md. Voice: utbildande, approachable "svengelska" matching slides (per D-02). Explain WHY + show examples + provide context (per D-01).

**Section 1: Introduktion**

### 1.1 Problemet med ostrukturerad AI-assistans
Expand from slides "Känner du igen det här?" and "Det som saknas":
- Open with the relatable developer frustration: explaining architecture three times in three different chats
- Cover the three core problems in depth (not just bullet points):
  1. **Kontext** — each session starts from zero, decisions/architecture/progress lost
  2. **Struktur** — no plan, no order, AI does its best but loses the thread
  3. **Kvalitet** — "tasks done!" but nobody checks if the goal is actually met
- Add the progression narrative from slides: `Manuellt → IDE → Linter → CI → AI-assistent` — each step automated the boring parts, AI is the next step in that chain
- Use a blockquote for the key insight: "Verktygen är kraftfulla. Det som saknas är ett system."
- Do NOT reproduce slides word-for-word — expand with more context and examples (per D-03)
- Target ~500-700 words for this subsection

### 1.2 Vad är ett agent framework?
Expand from slides "Vad är ett AI agent framework?":
- Explain the four components (LLM, Instruktioner, Verktyg, Minne) with a brief paragraph for each — not just labels
- Include the ASCII diagram from slides (the box with 4 components) or adapt it for the companion
- Contrast: "Utan framework: ChatGPT med copy-paste. Med framework: strukturerad assistent som minns." — expand on WHY memory and structure change the game
- This section must stand alone (per COMP-03/D-06) — a reader who hasn't seen the slides must fully understand the concept
- Target ~400-600 words for this subsection

**Section 2: GSD i korthet**

### 2.1 Arkitektur: orchestrator + agents
Expand from slides "GSD — tre byggstenar" and "18 agents, 4 roller":
- Explain the three building blocks: Orchestrator (coordinates, never builds), Specialized agents (each expert in their role), Persistent memory (.planning/)
- Include the agent architecture ASCII diagram from slides showing Orchestrator → Research(4), Planning(4), Execute(1), Verify(3) + 6 specialists
- Explain each role category briefly:
  - Research agents: investigate domain, libraries, patterns
  - Planning agents: create plans with tasks, dependency graphs, waves
  - Execute agent: implements the plan tasks
  - Verify agents: goal-backward verification (not just "files exist")
- Mention the 6 specialists briefly: debugger, codebase-mapper, UI team, etc.
- Target ~400-600 words

### 2.2 .planning/ — projektets minne
Expand from slides ".planning/ — projektets minne":
- Include the file tree diagram from slides showing PROJECT.md, REQUIREMENTS.md, ROADMAP.md, STATE.md, phases/
- Explain each file's role in 1-2 sentences:
  - PROJECT.md: vision, constraints, core value
  - REQUIREMENTS.md: all requirements with traceability to phases
  - ROADMAP.md: phases with success criteria and dependencies
  - STATE.md: current position, progress, decisions, blockers
  - phases/ subdirectories with CONTEXT.md, PLAN.md, SUMMARY.md etc.
- Emphasize: "Varje phase producerar artifacts. Inget försvinner — du kan alltid gå tillbaka och se vad som bestämdes och varför."
- Mention this is what enables session persistence — close VS Code, come back next week, GSD picks up where you left off
- Target ~300-500 words

### 2.3 Kärnprincip: du bestämmer, GSD strukturerar
- Articulate the core philosophy: developer decides WHAT and WHY, GSD handles HOW
- Reference the "Du bestämmer, AI utför" slide content: Du = vad ska byggas, arkitekturbeslut, kvalitetskrav. AI = research, planering, implementation, verification.
- Emphasize: "Utvecklaren blir arkitekt och reviewer — inte typist."
- Keep concise — this is a principle statement, not an essay
- Target ~200-300 words

**Format rules (per COMP-01):**
- Standard markdown only — NO Marp directives (`<!-- _class: -->`, `---` as slide separators, front matter)
- Use `#`-style headings, pipe tables, fenced code blocks (```bash for terminal examples)
- Use `$` prompt prefix for user commands, `✅` for success output in examples
- Blockquotes (`>`) sparingly for key insights
- NO HTML tags
- Reference "Copilot" only, never "Claude Code" (per D-13)
  </action>
  <verify>
    <automated>powershell -Command "$c = Get-Content companion.md -Raw; $checks = @(); $checks += if ($c -match 'Problemet med ostrukturerad') {'PASS: Section 1.1 header'} else {'FAIL: Missing Section 1.1'}; $checks += if ($c -match 'agent framework') {'PASS: Framework concept'} else {'FAIL: Missing framework concept'}; $checks += if ($c -match 'orchestrator') {'PASS: Orchestrator mentioned'} else {'FAIL: Missing orchestrator'}; $checks += if ($c -match '\.planning/') {'PASS: .planning/ explained'} else {'FAIL: Missing .planning/'}; $checks += if ($c -match 'du bestämmer') {'PASS: Core principle'} else {'FAIL: Missing core principle'}; $checks += if ($c -notmatch 'marp:|&lt;!-- _class') {'PASS: No Marp directives'} else {'FAIL: Marp directives found'}; $checks | ForEach-Object { Write-Host $_ }"</automated>
  </verify>
  <acceptance_criteria>
    - companion.md contains the heading `### Problemet med ostrukturerad AI-assistans`
    - companion.md contains the heading `### Vad är ett agent framework?`
    - companion.md contains the heading `### Arkitektur: orchestrator + agents` (or similar under Section 2)
    - companion.md contains the heading `### .planning/ — projektets minne`
    - companion.md contains the string `orchestrator` (lowercase)
    - companion.md contains the string `.planning/`
    - companion.md contains the string `du bestämmer`
    - companion.md does NOT contain `marp:` or `<!-- _class`
    - companion.md contains at least one ```bash code block with a `$` prompt example
    - companion.md contains the string `agent framework` (confirming concept is explained)
    - Section 1 content appears BEFORE Section 2 content (preserved order)
    - No forbidden Swedish terms from TERMINOLOGY.md appear (no `fas `, `vägkarta`, `arbetsflöde`, `orkestrerare`, `milstolpe`, `ramverk` used instead of English equivalents)
  </acceptance_criteria>
  <done>Sections 1 and 2 of companion.md contain comprehensive content covering the problem statement, agent framework concept, GSD architecture, .planning/ directory, and core principle — all in Swedish with English technical terms, no Marp directives, standalone readable.</done>
</task>

<task type="auto">
  <name>Task 2: Write Section 3 (Kom igång — On-Ramp)</name>
  <files>companion.md</files>
  <read_first>
    - companion.md (read the file again after Task 1 edits to see current state)
    - slides.md (lines 236-365 — Section 4: command spectrum, /gsd-fast, /gsd-quick, /gsd-do, full workflow, installation, first command, bridge slide)
    - TERMINOLOGY.md (language policy)
    - .planning/phases/06-companion-document/06-RESEARCH.md (Pattern 1: Slide-to-Companion Expansion, terminal example format)
  </read_first>
  <action>
Replace the placeholder headers for Section 3 with full content. This is the most practical "how-to" section — every subsection should have terminal examples showing real commands and output.

**Section 3: Kom igång — On-Ramp**

Open with a brief intro paragraph explaining the graduated complexity concept: you start with zero-friction commands and move to more structured ones as needs grow. Reference the command spectrum concept from slides.

### 3.1 /gsd-fast — triviala tasks
Expand from slides "/gsd-fast — noll overhead":
- Explain: simplest GSD command, no planning, no subagents, no .planning/ directory needed
- Terminal example (expand from slides, add a second example):
  ```bash
  $ /gsd-fast "add .env to .gitignore"
  ✅ Done: Added .env to .gitignore
     Commit: a3f2e1d
     Files: .gitignore
  ```
- Add a second example showing a different use case (e.g., fixing a typo, updating a dependency version)
- Explain constraints: max 3 file edits, atomic commit, logged in STATE.md if .planning/ exists
- "När du ska använda det:" bullet list — typos, config changes, you know exactly what needs doing, would take 2 minutes manually
- Target ~250-400 words

### 3.2 /gsd-do — smart routing
Expand from slides "/gsd-do — smart routing":
- Explain: describe what you want, GSD picks the right command
- Terminal example:
  ```bash
  $ /gsd-do "refactor the auth system"
  Routing to: /gsd-add-phase
  Reason: Multi-file refactor needs full phase
  ```
- Add another example showing it routing to /gsd-fast for a simple task
- Explain: never does the work itself — always routes to a specialist command
- "När du ska använda det:" — you're not sure which command fits, you want to describe the work naturally
- Target ~200-350 words

### 3.3 /gsd-quick — tasks med planering
Expand from slides "/gsd-quick — med planning":
- Explain: small tasks with GSD structure, planner + executor as subagents
- Terminal example:
  ```bash
  $ /gsd-quick "add dark mode toggle to settings page"
  Planning... 3 tasks identified
  Executing... ██████████ 3/3
  ✅ Done — dark mode toggle added
  ```
- Explain composable flags in detail (this is the key expansion beyond slides):
  - `--discuss`: adds a discussion phase before planning ("Jag vet inte alla detaljer")
  - `--research`: adds research agent investigation ("Jag vet inte bästa sättet")
  - `--full`: adds plan-checking + verification ("Det här måste bli rätt")
  - Show combined example: `/gsd-quick --discuss --research "add OAuth2 login"`
- "När du ska använda det:" — adding a feature, small refactor, anything bigger than 3-file change but not a full project
- Target ~350-500 words

### 3.4 Vilken ska jag välja? (beslutsträd)
Adapt from slides "Vilket kommando ska jag använda?":
- Include the ASCII decision tree from slides (the full tree with "What are you doing?" root):
  ```
  What are you doing?
  ├── Starting something new?
  │   ├── Whole project       → /gsd-new-project
  │   └── One feature         → /gsd-quick --discuss
  ├── Working on existing code?
  │   ├── First time here?    → /gsd-map-codebase
  │   ├── Bug or error?       → /gsd-debug
  │   └── Big refactor?       → /gsd-add-phase
  ├── Something small?
  │   ├── Trivial fix (≤3)    → /gsd-fast
  │   └── Not sure?           → /gsd-do
  └── Coming back?
      └── Resuming work       → /gsd-resume-work
  ```
- Add a brief prose paragraph BELOW the tree explaining the key principle: "Börja till vänster (enklare). Flytta höger (mer struktur) när du behöver mer."
- Keep the decision tree in English (as established in Phase 4 decision) with Swedish surrounding text
- DO NOT convert to long prose — keep the visual tree format (per Research pitfall 4)
- Target ~200-300 words (including tree)

Also add a brief installation subsection before the decision tree or at the start of Section 3:

### Förutsättningar och installation
From slides "Kom igång — förutsättningar":
- Node.js ≥18 + VS Code with GitHub Copilot
- Installation: `npx get-shit-done-cc@latest`
- First command example: `/gsd-fast "fix the typo in README"`
- Target ~150-200 words

**Format rules:**
- Same as Task 1 — standard markdown, no Marp directives
- Terminal examples: ```bash fence, `$` prompt, `✅` for success, indented detail lines
- Copilot-only references (per D-13)
  </action>
  <verify>
    <automated>powershell -Command "$c = Get-Content companion.md -Raw; $checks = @(); $checks += if ($c -match '/gsd-fast') {'PASS: /gsd-fast section'} else {'FAIL: Missing /gsd-fast'}; $checks += if ($c -match '/gsd-do') {'PASS: /gsd-do section'} else {'FAIL: Missing /gsd-do'}; $checks += if ($c -match '/gsd-quick') {'PASS: /gsd-quick section'} else {'FAIL: Missing /gsd-quick'}; $checks += if ($c -match 'beslutsträd|What are you doing') {'PASS: Decision tree'} else {'FAIL: Missing decision tree'}; $checks += if ($c -match '--discuss') {'PASS: Composable flags'} else {'FAIL: Missing composable flags'}; $checks += if ($c -match 'npx get-shit-done') {'PASS: Install command'} else {'FAIL: Missing install'}; $checks += if (($c | Select-String '```bash' -AllMatches).Matches.Count -ge 4) {'PASS: >=4 bash examples'} else {'FAIL: <4 bash examples'}; $checks | ForEach-Object { Write-Host $_ }"</automated>
  </verify>
  <acceptance_criteria>
    - companion.md contains `### /gsd-fast` (or equivalent heading under Section 3)
    - companion.md contains `### /gsd-do`
    - companion.md contains `### /gsd-quick`
    - companion.md contains `beslutsträd` or the decision tree ASCII art with `What are you doing?`
    - companion.md contains `--discuss` (composable flags explained)
    - companion.md contains `--research` (composable flags explained)
    - companion.md contains `--full` (composable flags explained)
    - companion.md contains `npx get-shit-done` (installation command)
    - companion.md contains at least 4 ```bash code blocks in Section 3
    - Section 3 content appears AFTER Section 2 content (preserved order)
    - No Marp directives added
  </acceptance_criteria>
  <done>Section 3 of companion.md contains comprehensive on-ramp content covering all four command levels (/gsd-fast, /gsd-do, /gsd-quick, full workflow), installation, composable flags, and the decision tree — with terminal examples and practical guidance for each command.</done>
</task>

</tasks>

<verification>
After both tasks complete, verify the full document structure:

```powershell
$c = Get-Content companion.md -Raw

# Structure check: all 3 sections present with content
$sections = @('## 1. Introduktion', '## 2. GSD i korthet', '## 3. Kom igång')
foreach ($s in $sections) {
  if ($c -match [regex]::Escape($s)) { Write-Host "PASS: $s found" }
  else { Write-Host "FAIL: $s missing" }
}

# No Marp directives
if ($c -match 'marp:|<!-- _class|<!-- _paginate') { Write-Host "FAIL: Marp directives found" }
else { Write-Host "PASS: No Marp directives" }

# Terminology spot-check (forbidden Swedish terms)
$forbidden = @('fas ', ' faserna', 'arbetsflöde', 'milstolpe', 'ramverk')
foreach ($term in $forbidden) {
  if ($c -match $term) { Write-Host "WARNING: Forbidden term '$term' found" }
}

# Content depth check: document should be substantially longer than outline
$lineCount = (Get-Content companion.md).Count
Write-Host "Line count: $lineCount (expect > 100 for Sections 1-3)"
```
</verification>

<success_criteria>
- Sections 1-3 of companion.md contain substantive content (not just headers)
- Document exceeds 100 lines (outline was ~44 lines)
- A reader unfamiliar with GSD can understand the basics from Sections 1-3 alone
- All terminal examples use ```bash with $ prompt format
- Language is consistently Swedish with English technical terms per TERMINOLOGY.md
- No Marp directives anywhere in the file
- Sections 4-7 and Appendix headers remain intact as placeholders for later plans
</success_criteria>

<output>
After completion, create `.planning/phases/06-companion-document/06-01-SUMMARY.md`
</output>
