---
phase: 06-companion-document
plan: 03
type: execute
wave: 3
depends_on: [06-01, 06-02]
files_modified: [companion.md]
autonomous: true
requirements: [COMP-02, PRAC-06]

must_haves:
  truths:
    - "Section 6 covers best practices including outcome thinking, prompt crafting, anti-patterns, escape hatches, and gradual adoption"
    - "Cross-AI review (/gsd-review) is explained via a realistic scenario where a developer uses a different AI to review Copilot's work (PRAC-06)"
    - "Section 7 contains a curated command reference table with 10-15 commands in Command | Description | När? format"
    - "Section 7 contains a .planning/ file structure overview with ASCII tree and explanations"
    - "Appendix contains an expanded GSD vs BMAD comparison at the philosophy level (4-5 dimensions)"
    - "The complete document renders as standard markdown without special tooling"
  artifacts:
    - path: "companion.md"
      provides: "Complete companion document — all 7 sections + appendix with full content"
      contains: "Kommandoöversikt"
  key_links:
    - from: "companion.md Section 6"
      to: "slides.md Section 7"
      via: "expanded best practices with /gsd-review integrated"
      pattern: "gsd-review"
    - from: "companion.md Section 7"
      to: "all prior sections"
      via: "curated command table referencing all commands introduced earlier"
      pattern: "Kommandoöversikt.*gsd-fast.*gsd-quick"
    - from: "companion.md Appendix"
      to: "slides.md Section 2"
      via: "expanded philosophy comparison"
      pattern: "GSD vs BMAD"
---

<objective>
Write Sections 6-7 and Appendix of the companion document: Best Practices (with cross-AI review), Reference (command table + .planning/ structure), and GSD vs BMAD comparison.

Purpose: Complete the companion document with actionable best practices, a practical quick-reference section, and a balanced framework comparison — making the document a self-contained reference.

Output: companion.md fully complete — all 7 sections + appendix filled with content. Document is standalone-readable.
</objective>

<execution_context>
@~/.copilot/get-shit-done/workflows/execute-plan.md
@~/.copilot/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/06-companion-document/06-CONTEXT.md
@.planning/phases/06-companion-document/06-RESEARCH.md
@.planning/phases/06-companion-document/06-01-SUMMARY.md
@.planning/phases/06-companion-document/06-02-SUMMARY.md

@companion.md
@slides.md
@TERMINOLOGY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Write Section 6 (Best Practices) with Cross-AI Review (PRAC-06)</name>
  <files>companion.md</files>
  <read_first>
    - companion.md (current state after Plans 01+02 — read to see Section 5 ending, locate Section 6 placeholders)
    - slides.md (lines 635-716 — Section 7: outcome thinking, du bestämmer/AI utför, prompt crafting, anti-patterns, escape hatches)
    - TERMINOLOGY.md (language policy)
    - .planning/phases/06-companion-document/06-CONTEXT.md (D-07: scenario-driven advanced features, D-08: cross-AI review in Section 6)
    - .planning/phases/06-companion-document/06-RESEARCH.md (Pattern 2: Scenario-Driven Advanced Features, section length ~600-800 words)
  </read_first>
  <action>
Replace the Section 6 placeholder headers with full content. This section is about mindset and practices — actionable principles with concrete examples. Write in Swedish with English technical terms per TERMINOLOGY.md.

**Section 6: Best Practices**

Open with a brief intro: having the right tools is half the job — the other half is how you think and communicate with the agent. These practices help you get the most out of GSD.

### 6.1 Tänk i outcomes
Expand from slides "Tänk outcomes, inte steg":
- Show the contrast (from slides but expanded):
  - ❌ Step-by-step: "Create a file called AuthService.ts, add a login method that accepts email and password, hash with bcrypt, create a JWT token..."
  - ✅ Outcome: "Användare ska kunna logga in med email och lösenord"
- Explain WHY outcomes work better: GSD derives the steps from the goal. You define what "done" looks like — the planning agent figures out the path.
- Add another example pair beyond what slides show (e.g., refactoring outcome vs step-by-step refactoring instructions)
- Reference the "Du bestämmer, AI utför" principle from Section 2
- Target ~200-350 words

### 6.2 Trust but verify
- Expand the verification mindset from the slides
- Key message: trust GSD to do the work, but always run verification
- Reference goal-backward verification from Section 4.5 — "task completion ≠ goal achievement"
- Practical advice: always use `/gsd-verify-work` or `--full` flag for important work
- Brief example of what happens when you skip verification (from slides' anti-pattern: "3/3 tasks done!" but nothing works)
- Target ~200-300 words

### 6.3 Prompt crafting — var specifik
Expand from slides "Prompt crafting — var specifik":
- Show the three prompt pairs from slides:
  - ❌ `"Fix the bug"` → ✅ `"The login form returns 401 when valid credentials are submitted"`
  - ❌ `"Add a button"` → ✅ `"Users should be able to export their data as CSV from the settings page"`
  - ❌ `"Refactor this"` → ✅ `"Refactor the auth module — it mixes JWT creation, validation, and user lookup in one file"`
- Explain the formula: **Kontext + mål + constraints = bättre resultat**
- Add 1-2 additional prompt pairs as companion-only examples
- Target ~200-350 words

### 6.4 Tre vanliga misstag (anti-patterns)
Expand from slides "Tre vanliga misstag":
- **🚫 Vaga prompts** — agenten gissar fel, du gör om arbetet → Var specifik: mål + kontext + constraints
- **🚫 Skippa verification** — "3/3 tasks done!" men inget fungerar → Kör alltid verification
- **🚫 Micro-management** — du dikterar varje fil och rad → Beskriv outcome, låt GSD planera vägen dit
- For each anti-pattern, add a brief "hur det ser ut i praktiken" example and "hur du undviker det" guidance (beyond what slides cover)
- Target ~250-400 words

### 6.5 När du INTE ska använda GSD (escape hatches)
Expand from slides "När du INTE ska använda agents":
- List the 5 escape hatches from slides with brief explanations:
  - 🚫 **Arkitekturbeslut** — agenten optimerar lokalt, du optimerar globalt
  - 🚫 **Security-kritisk kod** — alltid manuell review oavsett
  - 🚫 **Teamets lärande** — om syftet är att lära, inte leverera
  - 🚫 **Regelverk & compliance** — agenten förstår inte juridiskt ansvar
  - 🚫 **Det du inte kan reviewera** — delegera inte det du inte kan bedöma
- Use blockquote: "GSD gör dig effektivare — men det är fortfarande du som är ansvarig."
- Target ~200-300 words

### 6.6 Gradvis adoption
- Not directly a single slide — but a natural companion topic
- Explain the adoption path: start with /gsd-fast for a week, then try /gsd-quick for a feature, then a full project with /gsd-new-project
- Reference the "Start Monday" challenge from slides Section 8:
  - 5 min: Fixa en riktig typo → `/gsd-fast`
  - 30 min: Lägg till en feature → `/gsd-quick`
  - 2 timmar: Nytt sidoprojekt → `/gsd-new-project`
- Target ~200-300 words

### 6.7 Cross-AI review — `/gsd-review` (NEW — PRAC-06)
This content is NOT in the slides. Use the scenario-driven pattern from 06-RESEARCH.md (D-07). Place in Section 6 per D-08.

Write with this structure:
1. **Scenario opener:** "Du har låtit Copilot bygga en auth-modul genom GSD. Koden fungerar, verification passerar — men du vill ha en second opinion. Kanske en annan AI ser mönster eller problem som Copilot missade."
2. **Concept:** Cross-AI review uses a different AI model to review the work done by the primary agent — like a code review but across AI perspectives. Multiple reviewers provide independent feedback that is synthesized into consensus.
3. **Command example:**
   ```bash
   $ /gsd-review
   Reviewing Phase 2: Auth System
   Reviewer 1 (Claude): 3 concerns, 2 suggestions
   Reviewer 2 (GPT-4): 1 concern, 4 suggestions
   ✅ Consensus: 2 shared concerns identified
   ```
4. **Value proposition:** Different AI models have different strengths and blind spots. Cross-review catches issues that a single model might miss.
5. **"När du ska använda det:" list:**
   - Kritisk kod (auth, betalningar, datahantering)
   - Du vill validera arkitekturbeslut
   - Du har tid för en extra kvalitetskontroll
- Target ~250-400 words

**Format rules:** Same as previous plans — standard markdown, no Marp directives, bash terminal examples, Swedish prose with English technical terms, Copilot-only references (per D-13).
  </action>
  <verify>
    <automated>powershell -Command "$c = Get-Content companion.md -Raw; $checks = @(); $checks += if ($c -match 'Tänk i outcomes|outcomes') {'PASS: Outcomes thinking'} else {'FAIL: Missing outcomes'}; $checks += if ($c -match 'Trust but verify|verification') {'PASS: Trust but verify'} else {'FAIL: Missing trust/verify'}; $checks += if ($c -match 'Prompt crafting|var specifik') {'PASS: Prompt crafting'} else {'FAIL: Missing prompt crafting'}; $checks += if ($c -match 'vanliga misstag|anti-pattern') {'PASS: Anti-patterns'} else {'FAIL: Missing anti-patterns'}; $checks += if ($c -match 'INTE ska använda') {'PASS: Escape hatches'} else {'FAIL: Missing escape hatches'}; $checks += if ($c -match 'gsd-review') {'PASS: Cross-AI review (PRAC-06)'} else {'FAIL: Missing /gsd-review'}; $checks += if ($c -match 'Gradvis adoption|adoption') {'PASS: Gradual adoption'} else {'FAIL: Missing gradual adoption'}; $checks | ForEach-Object { Write-Host $_ }"</automated>
  </verify>
  <acceptance_criteria>
    - companion.md contains a subsection about outcome thinking with ❌/✅ prompt contrast examples
    - companion.md contains a "trust but verify" subsection referencing verification
    - companion.md contains prompt crafting examples with at least 3 ❌/✅ pairs
    - companion.md contains anti-patterns section with `Vaga prompts`, `Skippa verification`, `Micro-management`
    - companion.md contains escape hatches with at least 4 items (architecture decisions, security, team learning, compliance)
    - companion.md contains the string `gsd-review` with a scenario-driven explanation (PRAC-06)
    - companion.md contains a cross-AI review scenario that opens with a developer situation (not a feature description)
    - companion.md contains the string `Gradvis adoption` or equivalent adoption path guidance
    - Section 6 content appears AFTER Section 5 and BEFORE Section 7 placeholders
    - No Marp directives added
  </acceptance_criteria>
  <done>Section 6 of companion.md contains comprehensive best practices covering outcome thinking, verification mindset, prompt crafting, anti-patterns, escape hatches, gradual adoption, and cross-AI review (/gsd-review with PRAC-06) — all with concrete examples and actionable guidance.</done>
</task>

<task type="auto">
  <name>Task 2: Write Section 7 (Referens) and Appendix (GSD vs BMAD)</name>
  <files>companion.md</files>
  <read_first>
    - companion.md (current state after Task 1 — read to see Section 6 ending, locate Section 7 and Appendix placeholders)
    - slides.md (lines 134-147 — Section 2: GSD vs BMAD philosophy table with 3 rows)
    - TERMINOLOGY.md (language policy)
    - .planning/phases/06-companion-document/06-CONTEXT.md (D-09: curated 10-15 commands, D-10: .planning/ file structure, D-11: table format)
    - .planning/phases/06-companion-document/06-RESEARCH.md (Pattern 3: command table format, Pattern 4: file structure overview, recommended 14 commands, appendix depth guidance)
  </read_first>
  <action>
Replace the Section 7 and Appendix placeholder headers with full content. Section 7 is the practical reference — scannable, quick-lookup. The Appendix expands the GSD vs BMAD comparison from the slides.

**Section 7: Referens**

### 7.1 Kommandoöversikt
Create a curated command reference table with the following 14 commands (per 06-RESEARCH.md recommended candidates, adjusted per agent discretion — D-09 allows 10-15):

Use this exact table format (per D-11):

```markdown
| Kommando | Beskrivning | När? |
|----------|-------------|------|
| `/gsd-fast` | Snabbfix utan planning — atomic commit | Triviala ändringar (≤3 filer) |
| `/gsd-quick` | Task med planner + executor, composable flags | Ny feature, liten refaktor |
| `/gsd-do` | Smart routing — väljer rätt kommando åt dig | Du vet inte vilket kommando du behöver |
| `/gsd-new-project` | Nytt projekt med full workflow | Greenfield — helt nytt projekt |
| `/gsd-plan-phase` | Planerar en phase med tasks och waves | Nästa phase i roadmap ska planeras |
| `/gsd-execute-phase` | Exekverar en planerad phase | Phase är planerad, redo att bygga |
| `/gsd-verify-work` | Goal-backward verification av phase | Phase exekverad, mål ska verifieras |
| `/gsd-resume-work` | Återuppta arbete efter paus | Du kommer tillbaka till ett projekt |
| `/gsd-progress` | Visa projektets status | Snabb koll — var står vi? |
| `/gsd-map-codebase` | Kartlägger befintlig kodbas | Brownfield — innan du börjar ändra |
| `/gsd-debug` | Systematisk buggutredning | Komplext problem som kräver analys |
| `/gsd-insert-phase` | Lägg till en phase mitt i roadmap | Saknat steg upptäckt under arbete |
| `/gsd-review` | Cross-AI review av utfört arbete | Kritisk kod — vill ha second opinion |
| `/gsd-autonomous` | Kör hela pipeline:n utan stopp | Erfaren användare, tydligt scope |
```

Add a brief intro sentence before the table: "De viktigaste GSD-kommandona — från enklaste till mest avancerade."

Add a brief note after the table: "Alla kommandon körs i Copilot Chat. Composable flags (`--discuss`, `--research`, `--full`) fungerar med `/gsd-quick`."

### 7.2 .planning/ filstruktur
Create the file structure overview (per D-10). Use the ASCII tree format from 06-RESEARCH.md Pattern 4:

```
.planning/
├── PROJECT.md        # Vad vi bygger — vision, constraints, core value
├── REQUIREMENTS.md   # Alla krav med spårbarhet till phases
├── ROADMAP.md        # Phases med success criteria och beroenden
├── STATE.md          # Var projektet är just nu — progress, beslut
├── config.json       # GSD-konfiguration för detta projekt
└── phases/
    └── 01-namn/
        ├── 01-CONTEXT.md       # Beslut från /gsd-discuss-phase
        ├── 01-RESEARCH.md      # Domänresearch från /gsd-research-phase
        ├── 01-PLAN.md          # Executionsplan med tasks
        ├── 01-SUMMARY.md       # Resultat efter execution
        └── 01-VERIFICATION.md  # Goal-backward verification
```

Below the tree, add a brief explanation (2-3 sentences): "Varje phase producerar sin egen uppsättning artifacts. Inget försvinner — du kan alltid gå tillbaka och se vad som bestämdes och varför. Det här är det som skiljer GSD från en vanlig AI-chat: persistent memory som överlever sessioner, dagar och veckor."

### 7.3 Länkar och resurser
Add a brief resources section with:
- GSD installation: `npx get-shit-done-cc@latest`
- GitHub Copilot: link placeholder (or generic "GitHub Copilot i VS Code")
- TERMINOLOGY.md: reference to the project's own terminology file
- Keep this section very brief — 3-5 bullet items, no essay

**Appendix: GSD vs BMAD — kortjämförelse**

Expand from slides "Två filosofier" (3-row table: Approach, Agents, Styrka):
- Expand to 4-5 comparison dimensions at the PHILOSOPHY level (not a feature matrix per Phase 5 D-09):

```markdown
| Dimension | GSD | BMAD |
|-----------|-----|------|
| **Filosofi** | Workflow-centric — structured execution pipeline | Persona-based — specialized roles (PM, Architect, Dev) |
| **Agents** | Researcher, Planner, Executor, Verifier — funktionsroller | PM, Architect, Developer, QA — yrkesroller |
| **Styrka** | Execution + verification — bygger och kontrollerar | Strategi + dokumentation — planerar och dokumenterar |
| **Workflow** | Linear pipeline: discuss → plan → execute → verify | Role-based handoffs: each persona contributes their expertise |
| **Bäst för** | Projekt där execution och kvalitetskontroll är kritiskt | Projekt där strategisk planering och dokumentation är viktigast |
```

Add 2-3 paragraphs of prose below the table:
- These are different tools for different problems — not competitors
- GSD excels at structured execution with built-in quality gates
- BMAD excels at upfront strategy and comprehensive documentation
- "Vi valde GSD — execution med kvalitetskontroll" (echoing slides)
- Neither is "better" — the choice depends on what your project needs most
- Keep balanced and honest — not a sales pitch for GSD (per Phase 5 decisions)

Target ~300-500 words for the Appendix.

**Format rules:** Same as previous plans — standard markdown, no Marp directives. The command table must use pipe syntax (per D-11). No HTML tags. Swedish prose with English technical terms.
  </action>
  <verify>
    <automated>powershell -Command "$c = Get-Content companion.md -Raw; $checks = @(); $checks += if ($c -match 'Kommandoöversikt') {'PASS: Command table header'} else {'FAIL: Missing command table'}; $checks += if (($c | Select-String '/gsd-fast.*Snabbfix' -Quiet) -or ($c -match 'gsd-fast.*triviala|gsd-fast.*Snabbfix')) {'PASS: /gsd-fast in table'} else {'FAIL: /gsd-fast missing from table'}; $checks += if ($c -match 'filstruktur') {'PASS: File structure section'} else {'FAIL: Missing file structure'}; $checks += if ($c -match 'PROJECT\.md.*REQUIREMENTS\.md|REQUIREMENTS\.md.*ROADMAP\.md') {'PASS: .planning/ files listed'} else {'FAIL: .planning/ files not listed'}; $checks += if ($c -match 'GSD vs BMAD') {'PASS: Appendix comparison'} else {'FAIL: Missing BMAD comparison'}; $checks += if ($c -match 'Persona-based|persona-based') {'PASS: BMAD described'} else {'FAIL: BMAD not described'}; $checks += if ($c -notmatch 'marp:|<!-- _class') {'PASS: No Marp directives'} else {'FAIL: Marp directives found'}; $checks | ForEach-Object { Write-Host $_ }"</automated>
  </verify>
  <acceptance_criteria>
    - companion.md contains the heading `### Kommandoöversikt` (or equivalent under Section 7)
    - companion.md contains a markdown table with at least 10 commands including `/gsd-fast`, `/gsd-quick`, `/gsd-do`, `/gsd-new-project`, `/gsd-insert-phase`, `/gsd-review`, `/gsd-autonomous`
    - The command table has three columns: Kommando, Beskrivning, När?
    - companion.md contains `### .planning/ filstruktur` (or equivalent) with an ASCII file tree
    - The file tree includes `PROJECT.md`, `REQUIREMENTS.md`, `ROADMAP.md`, `STATE.md`
    - companion.md contains `## Appendix` with `### GSD vs BMAD`
    - The BMAD comparison table has at least 4 rows comparing dimensions
    - The comparison mentions `Persona-based` or `persona-based` for BMAD
    - The comparison mentions `Workflow-centric` or `workflow-centric` for GSD
    - companion.md does NOT contain `marp:` or `<!-- _class`
    - All 7 section headings (## 1 through ## 7) plus ## Appendix exist in the document
    - No forbidden Swedish terms from TERMINOLOGY.md in the document (spot-check: no `fas `, `arbetsflöde`, `milstolpe`, `ramverk` used instead of English equivalents)
  </acceptance_criteria>
  <done>Sections 6-7 and Appendix of companion.md are complete. The full companion document now contains all 7 sections + appendix with comprehensive content. Command reference table has 14 curated commands, .planning/ structure is documented, GSD vs BMAD comparison is balanced and philosophy-level. The document is standalone-readable and renders as standard markdown.</done>
</task>

</tasks>

<verification>
After both tasks complete, run the full document validation:

```powershell
$c = Get-Content companion.md -Raw

# All sections present
$sections = @('## 1. Introduktion', '## 2. GSD i korthet', '## 3. Kom igång', '## 4. Full Workflow', '## 5. Scenarion', '## 6. Best Practices', '## 7. Referens', '## Appendix')
foreach ($s in $sections) {
  if ($c -match [regex]::Escape($s)) { Write-Host "PASS: $s" }
  else { Write-Host "FAIL: $s missing" }
}

# All 3 advanced features
if ($c -match 'gsd-insert-phase') { Write-Host "PASS: PRAC-05" } else { Write-Host "FAIL: PRAC-05" }
if ($c -match 'gsd-review') { Write-Host "PASS: PRAC-06" } else { Write-Host "FAIL: PRAC-06" }
if ($c -match 'gsd-autonomous') { Write-Host "PASS: PRAC-07" } else { Write-Host "FAIL: PRAC-07" }

# No Marp directives
if ($c -match 'marp:|<!-- _class|<!-- _paginate') { Write-Host "FAIL: Marp directives" }
else { Write-Host "PASS: No Marp directives" }

# Command table
if ($c -match 'Kommandoöversikt') { Write-Host "PASS: Command table" }
else { Write-Host "FAIL: Command table" }

# TERMINOLOGY.md compliance
$forbidden = @('fas ', ' faserna', 'arbetsflöde', 'milstolpe', 'ramverk')
$violations = 0
foreach ($term in $forbidden) {
  if ($c -match $term) { Write-Host "WARNING: '$term' found"; $violations++ }
}
if ($violations -eq 0) { Write-Host "PASS: Terminology compliance" }

# Final line count
$lineCount = (Get-Content companion.md).Count
Write-Host "Total lines: $lineCount (expect > 350 for complete document)"
```
</verification>

<success_criteria>
- companion.md is complete with all 7 sections + appendix containing substantive content
- All 6 phase requirements are satisfied: COMP-01 (standard markdown), COMP-02 (deeper than slides), COMP-03 (standalone readable), PRAC-05 (phase insertion), PRAC-06 (cross-AI review), PRAC-07 (autonomous mode)
- Command reference table has 10-15 curated commands in proper format
- .planning/ file structure is documented with ASCII tree
- GSD vs BMAD comparison is balanced and philosophy-level
- No Marp directives anywhere in document
- Consistent Swedish with English technical terms throughout
- Document renders correctly as standard markdown (no HTML, no special tooling)
</success_criteria>

<output>
After completion, create `.planning/phases/06-companion-document/06-03-SUMMARY.md`
</output>
