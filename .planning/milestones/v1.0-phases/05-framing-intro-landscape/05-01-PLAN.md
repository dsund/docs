---
phase: 05-framing-intro-landscape
plan: "01"
type: execute
wave: 1
depends_on: []
files_modified: [slides.md]
autonomous: true
requirements: [SLIDE-02, CTXT-01, CTXT-02, CTXT-03, CTXT-04]

must_haves:
  truths:
    - "Section 1 opens with relatable ChatGPT/Copilot Chat frustrations the audience has experienced personally"
    - "A collaboration reframe slide appears before any GSD content, positioning AI as natural evolution of dev tools"
    - "Section 2 defines 'AI agent framework' with shared vocabulary (LLM + instructions + tools + memory)"
    - "GSD vs BMAD comparison is philosophy-level only (workflow-centric vs persona-based), balanced and fair"
    - "No fear framing — no 'am I being replaced?' or negative AI disruption language"
    - "The total slide deck has ~51-53 slides appropriate for ~60 minutes of presentation"
  artifacts:
    - path: "slides.md"
      provides: "Section 1 and Section 2 content slides"
      contains: "Känner du igen"
  key_links:
    - from: "Section 1 problem slides"
      to: "Section 5 solutions (memory, verification)"
      via: "Problems stated in Section 1 are solved in Section 5"
      pattern: "glömmer|verification"
    - from: "Section 2 framework concept"
      to: "Section 3 agent architecture"
      via: "Concept defined in Section 2, detailed in Section 3"
      pattern: "agent framework"
    - from: "Section 2 last slide"
      to: "Section 3 divider"
      via: "Bridge text transitions to GSD deep-dive"
      pattern: "hur GSD fungerar|i praktiken"
---

<objective>
Write the opening slides for the presentation: Section 1 ("Var är vi idag?") with problem statement and collaboration reframe, and Section 2 ("Vad finns?") with agent framework concept and GSD vs BMAD philosophy contrast.

Purpose: These sections earn the audience's permission to go deep — by validating their pain, reframing AI as collaboration, and positioning GSD within the landscape before the technical deep-dive begins.

Output: slides.md updated with 5-7 new content slides across Section 1 and Section 2, bringing total deck to ~51-53 slides for ~60 minutes.
</objective>

<execution_context>
@~/.copilot/get-shit-done/workflows/execute-plan.md
@~/.copilot/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/05-framing-intro-landscape/05-CONTEXT.md
@.planning/phases/05-framing-intro-landscape/05-RESEARCH.md
@TERMINOLOGY.md
@slides.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Write Section 1 slides — problem statement and collaboration reframe</name>
  <files>slides.md</files>
  <read_first>
    - slides.md (current state — verify insertion point at line 63 comment, see existing Section 7 "Du bestämmer, AI utför" slide at ~line 587 to AVOID duplicating that framing)
    - TERMINOLOGY.md (English terms to use: context window, verification, prompt, agent, framework)
    - .planning/phases/05-framing-intro-landscape/05-CONTEXT.md (decisions D-01 through D-06, D-12 through D-17)
    - .planning/phases/05-framing-intro-landscape/05-RESEARCH.md (Section 1 narrative arc, slide patterns, content examples, anti-patterns)
  </read_first>
  <action>
Insert 3 content slides into slides.md after the Section 1 comment (`<!-- Section 1: The Problem — slides added by Phase 5 -->`), replacing the blank line + `---` that currently follows it. Each slide separated by `---`.

**Slide 1 — Problem recognition (CTXT-01, per D-03):**
Title: `## Känner du igen det här?`
Opening quote: `"Jag har förklarat arkitekturen tre gånger — i tre olika chattar."`
Bullet list of 4 relatable ChatGPT/Copilot Chat frustrations:
- Ny chat = ny kontext. Varje gång.
- AI:n glömmer beslut du tog igår
- Ingen verification — "3 filer skapade!" men fungerar det?
- Copy-paste av samma prompt, om och om igen
Closing blockquote: `> Ju större uppgiften, desto mer faller isär.`

**Slide 2 — Gap deepening (CTXT-01):**
Title: `## Det som saknas`
Frame as ceiling, not failure. Three concrete gaps as bullet points with brief explanations:
- **Kontext** — varje session börjar från noll. Beslut, arkitektur, progress — borta.
- **Struktur** — ingen plan, ingen ordning. AI:n gör sitt bästa men tappar tråden.
- **Kvalitet** — "tasks klara!" men ingen kollar om målet faktiskt är uppnått.
Closing blockquote: `> Verktygen är kraftfulla. Det som saknas är ett system.`

**Slide 3 — Collaboration reframe + bridge (CTXT-04, per D-04, D-05, D-06):**
Title: `## Nästa steg i en bekant resa`
Positive-first framing. Use the evolution analogy (NOT "Du bestämmer, AI utför" which is already in Section 7). Show progression:
```
Manuellt → IDE → Linter → CI → **AI-assistent**
```
Brief text: Varje steg automatiserade det tråkiga och lät utvecklaren fokusera på det som spelar roll. AI-assistenter är nästa steg — inte en ersättning, utan en **arbetsfördelning.**
Bridge line as blockquote: `> Det finns ett namn för det här: agent frameworks.`

**Edit mechanics:** Use the edit tool on slides.md. The old_str starts with `<!-- Section 1: The Problem — slides added by Phase 5 -->` and goes through the blank line + slide separator (`---`) + blank line + `<!-- _class: divider -->` + `<!-- _paginate: skip -->` + blank line + `# Vad finns?`. Replace with the 3 new content slides (each preceded by a slide separator) followed by the Section 2 divider markup (preserving `<!-- _class: divider -->`, `<!-- _paginate: skip -->`, `# Vad finns?`, `<hr>`). Each content slide uses standard slide separator. No `_class` or `_paginate` directives on content slides (only dividers use those, per established pattern). Swedish text with English technical terms per TERMINOLOGY.md (use "verification" not "verifiering", "kontext" is OK per Swedish usage but "context window" stays English, "prompt" stays English).
  </action>
  <verify>
    <automated>powershell -Command "Select-String -Pattern 'Känner du igen' slides.md; Select-String -Pattern 'Det som saknas' slides.md; Select-String -Pattern 'bekant resa' slides.md; Select-String -Pattern 'agent frameworks' slides.md; if (Select-String -Pattern 'ersätter AI mig|am I being replaced' slides.md) { Write-Error 'Fear framing detected'; exit 1 }"</automated>
  </verify>
  <acceptance_criteria>
    - slides.md contains `## Känner du igen det här?` as a slide heading
    - slides.md contains `"Jag har förklarat arkitekturen tre gånger"` as a quote
    - slides.md contains `## Det som saknas` as a slide heading
    - slides.md contains `## Nästa steg i en bekant resa` (or similar evolution-themed heading)
    - slides.md contains the progression `Manuellt → IDE → Linter → CI` (evolution analogy)
    - slides.md contains bridge text referencing "agent frameworks"
    - slides.md does NOT contain "ersätter AI mig" or "am I being replaced" anywhere in Section 1
    - slides.md does NOT contain `_class:` or `_paginate:` directives on any Section 1 content slides (only the divider has those)
    - The Section 1 comment `<!-- Section 1: The Problem -->` is preserved
    - The Section 2 divider (`# Vad finns?`) immediately follows the last Section 1 content slide's `---` separator
    - 3 new `---` separators exist between the Section 1 comment and the Section 2 divider (3 content slides)
  </acceptance_criteria>
  <done>Section 1 has 3 content slides: problem recognition with ChatGPT/Copilot Chat frustrations, gap deepening (context/structure/quality), and collaboration reframe using evolution analogy — all in Swedish with English technical terms, positive-first framing, no fear language</done>
</task>

<task type="auto">
  <name>Task 2: Write Section 2 slides — agent framework concept and GSD vs BMAD comparison</name>
  <files>slides.md</files>
  <read_first>
    - slides.md (AFTER Task 1 — verify Section 1 slides are in place, find Section 2 insertion point at `<!-- Section 2: Framework Landscape -->` comment, verify Section 3 divider follows)
    - TERMINOLOGY.md (English terms: framework, agent, workflow, verification)
    - .planning/phases/05-framing-intro-landscape/05-CONTEXT.md (decisions D-07 through D-11)
    - .planning/phases/05-framing-intro-landscape/05-RESEARCH.md (Section 2 narrative arc, BMAD philosophy contrast table, concept slide pattern, anti-patterns to avoid)
  </read_first>
  <action>
Insert 2 content slides into slides.md after the Section 2 comment (`<!-- Section 2: Framework Landscape — slides added by Phase 5 -->`), replacing the blank line + `---` that currently follows it.

**Slide 1 — Agent framework concept (CTXT-02, per D-10, D-11: deductive flow — define concept FIRST):**
Title: `## Vad är ett AI agent framework?`
ASCII art building blocks visual in a plain code fence (no language specifier — per Phase 2 pattern for ASCII art):
```
  ┌──────────────────────────────────────┐
  │           AGENT FRAMEWORK            │
  │                                      │
  │  LLM            Instruktioner        │
  │  (hjärnan)      (hur den jobbar)     │
  │                                      │
  │  Verktyg        Minne                │
  │  (vad den kan)  (vad den kommer      │
  │                  ihåg)               │
  └──────────────────────────────────────┘
```
Below the diagram, one line of contrast:
`Utan framework: ChatGPT med copy-paste.`
`Med framework: **strukturerad assistent som minns.**`

**Slide 2 — GSD vs BMAD philosophy contrast + bridge (CTXT-03, per D-07, D-08, D-09):**
Title: `## Två filosofier`
Clean markdown table with exactly 3 data rows:

| | GSD | BMAD |
|---|---|---|
| **Approach** | Workflow-centric | Persona-based |
| **Agents** | Researcher, Planner, Executor, Verifier | PM, Architect, Developer, QA |
| **Styrka** | Execution + verification | Strategi + dokumentation |

Below table: `Olika problem, olika verktyg.`
Next line: `Vi valde GSD — **execution med kvalitetskontroll.**`
Bridge as closing blockquote: `> Låt oss se hur GSD fungerar i praktiken.`

**Critical constraints (per D-07, D-08, D-09):**
- NO mention of Cursor, aider, Windsurf, Claude Code, or Roo Code — GSD and BMAD only
- NO feature-level comparison (agent counts, specific commands, technical details)
- Balanced tone — "Olika problem, olika verktyg" not "GSD is better"
- Philosophy contrast only: workflow-centric vs persona-based

**Edit mechanics:** Use the edit tool on slides.md. The old_str starts with `<!-- Section 2: Framework Landscape — slides added by Phase 5 -->` and goes through the blank line + slide separator (`---`) + blank line + `<!-- _class: divider -->` + `<!-- _paginate: skip -->` + blank line + `# Hur fungerar det?`. Replace with the 2 new content slides (each preceded by a slide separator) followed by the Section 3 divider markup (preserving `<!-- _class: divider -->`, `<!-- _paginate: skip -->`, `# Hur fungerar det?`, `<hr>`). No `_class` or `_paginate` directives on content slides. Swedish text with English terms per TERMINOLOGY.md.
  </action>
  <verify>
    <automated>powershell -Command "Select-String -Pattern 'AGENT FRAMEWORK' slides.md; Select-String -Pattern 'Två filosofier' slides.md; Select-String -Pattern 'GSD.*BMAD' slides.md; Select-String -Pattern 'i praktiken' slides.md; if (Select-String -Pattern 'Cursor|aider|Windsurf|Claude Code|Roo Code' slides.md) { Write-Error 'Forbidden framework mention detected'; exit 1 }; $slides = (Get-Content slides.md -Raw); $count = ([regex]::Matches($slides, '(?m)^---\s*$')).Count + 1; Write-Output \"Total slides: $count (target: 49-53)\""</automated>
  </verify>
  <acceptance_criteria>
    - slides.md contains `## Vad är ett AI agent framework?` as a slide heading
    - slides.md contains ASCII art with `AGENT FRAMEWORK` and `LLM`, `Instruktioner`, `Verktyg`, `Minne` labels
    - slides.md contains `## Två filosofier` as a slide heading
    - slides.md contains a markdown table with columns `GSD` and `BMAD`
    - slides.md contains `Workflow-centric` and `Persona-based` in the comparison table
    - slides.md contains `Olika problem, olika verktyg` (balanced framing)
    - slides.md contains `execution med kvalitetskontroll` (reason for choosing GSD)
    - slides.md contains bridge text `Låt oss se hur GSD fungerar i praktiken` connecting to Section 3
    - slides.md does NOT contain any mention of Cursor, aider, Windsurf, Claude Code, or Roo Code
    - The comparison table has exactly 3 data rows (Approach, Agents, Styrka) — philosophy only, no feature matrix
    - No `_class:` or `_paginate:` directives on Section 2 content slides
    - The Section 2 comment `<!-- Section 2: Framework Landscape -->` is preserved
    - The Section 3 divider (`# Hur fungerar det?`) immediately follows the last Section 2 slide's `---` separator
    - Total slide count (count `---` separators + 1) is between 49 and 53
  </acceptance_criteria>
  <done>Section 2 has 2 content slides: agent framework concept with ASCII building blocks visual, and GSD vs BMAD philosophy contrast table with balanced framing and bridge to Section 3 — no forbidden framework mentions, philosophy-level comparison only</done>
</task>

</tasks>

<verification>
After both tasks complete:

1. **Marp renders without errors:**
   ```bash
   npx @marp-team/marp-cli slides.md --html -o /dev/null 2>&1
   ```

2. **Slide count in range:**
   Count `---` separators in slides.md — total should yield 49-53 slides

3. **Section structure intact:**
   - Title → Section 1 divider → 3 content slides → Section 2 divider → 2 content slides → Section 3 divider → (existing content)

4. **No forbidden content:**
   - No "ersätter AI mig" or fear framing
   - No Cursor, aider, Windsurf, Claude Code, Roo Code mentions
   - No `_class` directives on content slides

5. **Narrative flow:**
   - Section 1 ends with "agent frameworks" bridge → Section 2 opens with "Vad är ett AI agent framework?" (clean handoff)
   - Section 2 ends with "i praktiken" bridge → Section 3 opens with "Hur fungerar det?" (clean handoff)
</verification>

<success_criteria>
- slides.md has 5 new content slides (3 in Section 1, 2 in Section 2)
- Total deck is ~49-53 slides appropriate for ~60 minutes
- Section 1 validates developer pain and reframes AI as collaboration evolution
- Section 2 defines agent frameworks and positions GSD vs BMAD at philosophy level
- All content follows established Marp patterns (spacious, one idea per slide, Swedish + English terms)
- Section transitions are smooth (problem → concept → deep-dive)
</success_criteria>

<output>
After completion, create `.planning/phases/05-framing-intro-landscape/05-01-SUMMARY.md`
</output>
