---
phase: "03-core-payload-full-workflow"
plan: "01"
type: execute
wave: 1
depends_on: []
files_modified:
  - "slides.md"
autonomous: true
requirements:
  - "DEEP-02"
  - "DEEP-03"
must_haves:
  truths:
    - "Section 3 shows GSD agent architecture with orchestrator delegating to 4 categories (Research, Planning, Execute, Verify)"
    - "Section 3 shows wave-based parallel execution concept with plans grouped into sequential waves"
    - "Section 3 shows .planning/ directory file tree as persistent project memory"
    - "All 4 slides use Swedish text with English technical terms per TERMINOLOGY.md"
  artifacts:
    - path: "slides.md"
      provides: "4 new content slides in Section 3 between Section 3 divider and Section 4 divider"
      contains: "ORCHESTRATOR"
  key_links:
    - from: "Section 3 last slide (.planning/ directory)"
      to: "Section 4 divider (Börja enkelt)"
      via: "narrative thread — mental model before hands-on commands"
      pattern: "projektets minne"
---

<objective>
Insert 4 slides into Section 3 ("Hur fungerar det?") of the Marp slide deck, explaining GSD's mental model: the three pillars (orchestrator, agents, memory), agent architecture with the orchestrator pattern, wave-based execution, and the .planning/ directory as project memory.

Purpose: Give the audience the conceptual foundation before they see commands (Section 4) and the full workflow (Section 5). "Before I show you the commands, here's how GSD thinks."

Output: 4 new content slides in slides.md Section 3.
</objective>

<execution_context>
@~/.copilot/get-shit-done/workflows/execute-plan.md
@~/.copilot/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-core-payload-full-workflow/03-RESEARCH.md
@.planning/phases/03-core-payload-full-workflow/03-CONTEXT.md
@TERMINOLOGY.md
@slides.md

<interfaces>
<!-- Key patterns from Phase 1/2 that executor must follow -->

From slides.md (existing structure):
- Front matter: marp: true, theme: default, paginate: true, size: 16:9
- Section dividers use: <!-- _class: divider --> with <hr> tag
- Content slides: standard --- separator, NO _class directives, NO _paginate skip
- Code blocks styled at 0.85em via existing CSS
- Section 3 insertion point: after comment <!-- Section 3: GSD Mental Model — slides added by Phase 3 --> (line 85)
- Section 4 divider starts at the --- on line 87, followed by <!-- _class: divider --> / # Börja enkelt
- ASCII art uses plain code fence (no language specifier) to prevent syntax highlighting
- One idea per slide — spacious layout (per D-12)

From TERMINOLOGY.md (language rules):
- English terms used: phase, workflow, agent, orchestrator, execution, verification, planning, codebase
- Swedish grammar with English nouns: "i en phase", "workflow-steget"
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Insert Section 3 mental model slides (4 slides)</name>
  <files>slides.md</files>
  <read_first>
    - slides.md (find Section 3 insertion point at `<!-- Section 3: GSD Mental Model — slides added by Phase 3 -->` around line 85, and the Section 4 divider that follows)
    - .planning/phases/03-core-payload-full-workflow/03-RESEARCH.md (ASCII art diagrams in "Code Examples" section starting around line 442, agent categories table around line 198, slide recommendations around line 117)
    - TERMINOLOGY.md (language policy — use "orchestrator" not "orkestrerare", "agents" not "agenter", "phase" not "fas")
    - .planning/phases/02-core-payload-on-ramp/02-01-SUMMARY.md (patterns: no _class on content slides, plain code fence for ASCII art)
  </read_first>
  <action>
Insert 4 new content slides into slides.md between the Section 3 comment and the Section 4 divider. The edit replaces the transition from the comment to the divider, inserting slides in between while preserving the Section 4 divider intact.

**Edit operation:** Replace the block from `<!-- Section 3: GSD Mental Model — slides added by Phase 3 -->` through the `---` separator and up to (but not including) `<!-- _class: divider -->` for "Börja enkelt". Insert the 4 slides after the comment, each separated by `---`, with a final `---` before the Section 4 divider.

**Formatting rules (per Phase 1/2 patterns):**
- Each slide separated by `---` on its own line
- Content slides use `##` titles, NO `<!-- _class: ... -->` directives
- ASCII art in plain code fence (no language specifier) to prevent syntax highlighting
- Swedish text with English technical terms per TERMINOLOGY.md
- One idea per slide (per D-12)
- Keep ASCII art lines under 75 characters for projector readability

**Slide 1 — "GSD — tre byggstenar" (per D-03: conceptual overview):**

```markdown
---

## GSD — tre byggstenar

**Orchestrator** — koordinerar, bygger aldrig själv

**Specialiserade agents** — var och en expert på sin roll

**Persistent memory** — `.planning/` sparar allt mellan sessioner

> En AI som minns var du var, vad du bestämde, och vad som är kvar.
```

- Three bold items, one per core concept
- Blockquote as memorable one-liner footer
- No code blocks — pure conceptual slide

**Slide 2 — "18 agents, 4 roller" (DEEP-02 primary, per D-04: show categories not catalog):**

```markdown
---

## 18 agents, 4 roller

```
  ┌─────────────────────────────────────────┐
  │            ORCHESTRATOR                  │
  │     Koordinerar — bygger aldrig själv    │
  └────┬──────┬──────────┬──────────┬───────┘
       │      │          │          │
       ▼      ▼          ▼          ▼
  ┌────────┐ ┌────────┐ ┌────────┐ ┌────────┐
  │Research│ │Planning│ │Execute │ │Verify  │
  │  (4)   │ │  (4)   │ │  (1)   │ │  (3)   │
  └────────┘ └────────┘ └────────┘ └────────┘
  Undersöker  Skapar     Bygger    Kontrollerar
  domänen     planer     koden     kvalitet
```

\+ 6 specialister: debugger, codebase-mapper, UI-team m.fl.
```

- Plain code fence (NO language specifier) for the ASCII art
- Numbers in parentheses show agent count per category (4+4+1+3 = 12 core, 6 specialists)
- The `+` line outside the code block mentions remaining agents without listing them
- Box-drawing characters: ─ │ ┌ ┐ └ ┘ ├ ┤ ┬ ┴ (U+2500 series)
- Arrow characters: ▼ (U+25BC)

**Slide 3 — "Wave-baserad execution" (DEEP-02 wave execution, per D-05: concept without implementation):**

```markdown
---

## Wave-baserad execution

```
  Phase 2: Auth System
  ────────────────────

  Wave 1    [Plan A: Models]  [Plan B: Utils]
            ────────────────  ───────────────
                    ↓ klart

  Wave 2    [Plan C: Routes + middleware]
            ─────────────────────────────
                    ↓ klart

  Verify    Goal-backward: "Kan en användare logga in?"
```

Plans inom samma wave körs parallellt. Waves körs i ordning.
```

- Plain code fence (NO language specifier)
- Shows a concrete example (auth system) not abstract boxes
- Two waves demonstrate the concept: independent plans run in parallel, dependent plans wait
- Footer line summarizes the rule
- Arrow: ↓ (U+2193)

**Slide 4 — "`.planning/` — projektets minne" (DEEP-03 intro, per D-06/D-07: file tree + brief):**

```markdown
---

## `.planning/` — projektets minne

```
.planning/
├── PROJECT.md        ← Vad vi bygger
├── REQUIREMENTS.md   ← Krav + spårbarhet
├── ROADMAP.md        ← Phaser + success criteria
├── STATE.md          ← Var vi är just nu
└── phases/
    ├── 01-foundation/
    │   ├── 01-CONTEXT.md    ← Beslut
    │   ├── 01-PLAN.md       ← Plan
    │   └── 01-SUMMARY.md    ← Resultat
    └── 02-auth-system/
        └── ...
```

Varje phase producerar artifacts. Inget försvinner.
```

- Plain code fence (NO language specifier) for the file tree
- Tree-drawing characters: ├ └ │ ─ (standard tree format)
- Swedish annotations after ← arrows explain each file's purpose
- Shows 2 phases to illustrate the pattern without over-detailing
- Footer line reinforces persistence theme

**CRITICAL:** After inserting all 4 slides, verify that:
1. The Section 4 divider (`<!-- _class: divider -->` + `# Börja enkelt`) is still intact
2. There is exactly one `---` between the last new slide and the Section 4 divider
3. No `<!-- _class: ... -->` appears on any of the 4 new content slides
4. The Section 3 comment is preserved (don't delete it)
  </action>
  <verify>
    <automated>powershell -Command "$c = Get-Content slides.md -Raw; $fail = @(); if ($c -notmatch 'ORCHESTRATOR') { $fail += 'Missing ORCHESTRATOR' }; if ($c -notmatch '18 agents') { $fail += 'Missing 18 agents title' }; if ($c -notmatch 'Wave-baserad') { $fail += 'Missing Wave-baserad' }; if ($c -notmatch 'projektets minne') { $fail += 'Missing projektets minne' }; if ($c -notmatch 'tre byggstenar') { $fail += 'Missing tre byggstenar' }; if ($c -notmatch 'Börja enkelt') { $fail += 'Section 4 divider broken' }; if ($fail.Count -eq 0) { Write-Output 'PASS: All 4 Section 3 slides present and Section 4 intact' } else { $fail | ForEach-Object { Write-Output \"FAIL: $_\" }; exit 1 }"</automated>
  </verify>
  <acceptance_criteria>
    - slides.md contains `## GSD — tre byggstenar` between Section 3 comment and Section 4 divider
    - slides.md contains `## 18 agents, 4 roller` with `ORCHESTRATOR` in an ASCII art code block
    - slides.md contains `## Wave-baserad execution` with `Wave 1` and `Wave 2` in ASCII art
    - slides.md contains `## ` followed by `.planning/` with directory tree showing `PROJECT.md`, `ROADMAP.md`, `STATE.md`, `REQUIREMENTS.md`
    - All 4 slides use `##` titles (not `#` or `###`)
    - No `<!-- _class: ... -->` directives appear on any of the 4 new content slides
    - ASCII art diagrams use plain code fence (triple backticks with no language specifier)
    - Swedish text with English terms per TERMINOLOGY.md (e.g., "orchestrator" not "orkestrerare")
    - Each slide separated by `---` on its own line
    - Section 4 divider `# Börja enkelt` remains intact after the new slides
    - Section 3 comment `<!-- Section 3: GSD Mental Model — slides added by Phase 3 -->` is preserved
  </acceptance_criteria>
  <done>4 content slides inserted in Section 3 of slides.md showing GSD's three pillars, orchestrator + 4 agent categories ASCII art diagram, wave-based execution ASCII art, and .planning/ directory file tree. Section 4 divider and all existing Phase 2 slides remain intact.</done>
</task>

</tasks>

<verification>
Run all checks:

```powershell
$c = Get-Content slides.md -Raw
$checks = @{
  'DEEP-02 architecture' = ($c -match 'ORCHESTRATOR' -and $c -match '18 agents.*4 roller')
  'DEEP-02 waves' = ($c -match 'Wave-baserad' -and $c -match 'Wave 1' -and $c -match 'Wave 2')
  'DEEP-03 directory' = ($c -match '\.planning/' -and $c -match 'projektets minne')
  'Section 4 intact' = ($c -match 'Börja enkelt' -and $c -match 'Command Spectrum')
  'Phase 2 content intact' = ($c -match 'gsd-fast.*noll overhead' -and $c -match 'Men om uppgiften är större')
}
$checks.GetEnumerator() | ForEach-Object { Write-Output "$($_.Key): $(if ($_.Value) {'PASS'} else {'FAIL'})" }
```
</verification>

<success_criteria>
- 4 new slides in Section 3 of slides.md with correct Marp formatting
- ASCII art diagrams use box-drawing characters and render in monospace
- Swedish body text with English technical terms throughout
- Existing Phase 2 slides (Section 4 command spectrum, terminal examples, gateway bridge) remain unchanged
- Slide deck structure intact (all 8 section dividers present)
</success_criteria>

<output>
After completion, create `.planning/phases/03-core-payload-full-workflow/03-01-SUMMARY.md`
</output>
