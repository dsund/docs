---
phase: "02-core-payload-on-ramp"
plan: "01"
type: execute
wave: 1
depends_on: []
files_modified:
  - "slides.md"
autonomous: false
requirements:
  - "SLIDE-03"
  - "SLIDE-04"
  - "PRAC-01"
  - "PRAC-02"
  - "PRAC-03"
must_haves:
  truths:
    - "Command spectrum visual shows 4 levels (gsd-fast, gsd-quick, gsd-do, full workflow) in a single ASCII art slide"
    - "Each spectrum level has its own terminal example slide with $ prompt and plausible output"
    - "Installation slides show prerequisites, install command, and first command with zero-friction entry"
    - "Section ends with a gateway bridge slide that teases the full workflow"
    - "All slide text uses Swedish with English technical terms per TERMINOLOGY.md"
  artifacts:
    - path: "slides.md"
      provides: "8 new content slides in Section 4"
      contains: "Command Spectrum"
  key_links:
    - from: "slides.md new content"
      to: "slides.md Section 4 marker"
      via: "slides inserted after Section 4 comment"
      pattern: "Section 4.*Phase 2"
    - from: "slides.md Section 4 last slide"
      to: "slides.md Section 5 divider"
      via: "gateway bridge tease leads into Hela livscykeln"
      pattern: "Hela livscykeln"
---

<objective>
Create all content slides for Section 4 "Börja enkelt" — the GSD on-ramp section that shows the audience the command spectrum, proves each level with terminal examples, teaches installation, and bridges to the full workflow section.

Purpose: This is the "I could start using this today" section. It's the first content the audience sees (Phase 2 = first content phase built). The command spectrum slide is designed to be "the slide people photograph."

Output: 8 new slides inserted into slides.md Section 4, covering command spectrum visual, 4 terminal examples, 2 installation slides, and 1 gateway bridge.
</objective>

<execution_context>
@~/.copilot/get-shit-done/workflows/execute-plan.md
@~/.copilot/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-core-payload-on-ramp/02-CONTEXT.md
@.planning/phases/02-core-payload-on-ramp/02-RESEARCH.md
@TERMINOLOGY.md

<interfaces>
<!-- Key patterns from Phase 1 that executor must follow -->

From slides.md (existing structure):
- Front matter: marp: true, theme: default, paginate: true, size: 16:9
- Section dividers use: <!-- _class: divider --> with <hr> tag
- Content slides separated by --- on its own line
- Code blocks styled at 0.85em via existing CSS
- Section 4 insertion point: line 96, after comment <!-- Section 4: The On-Ramp — slides added by Phase 2 -->
- Next section divider (Section 5 "Hela livscykeln") starts with --- at line 98

From TERMINOLOGY.md (language rules):
- English terms used: phase, workflow, agent, commit, planning, execution, verification, codebase, debugging, on-ramp
- Swedish grammar with English nouns: "i en phase", "workflow-steget"
- Compound forms OK: "workflow-steget", "phase-strukturen"
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Command spectrum visual + terminal example slides</name>
  <files>slides.md</files>
  <read_first>
    - slides.md (see current Section 4 insertion point at line 96 and the --- separator + Section 5 divider that follows)
    - TERMINOLOGY.md (language policy — English terms in Swedish text)
    - .planning/phases/02-core-payload-on-ramp/02-RESEARCH.md (verified command details, ASCII art pattern at line 119-133, terminal examples at lines 189-266)
  </read_first>
  <action>
Insert 5 new content slides into slides.md AFTER the line `<!-- Section 4: The On-Ramp — slides added by Phase 2 -->` (line 96) and BEFORE the existing `---` that leads to the Section 5 divider.

Each slide starts with `---` on its own line. No `_class` directives on content slides (only dividers use those).

**Slide 1 — Command Spectrum Visual** (SLIDE-03, PRAC-03, per D-01/D-02/D-03/D-04):

```markdown
---

## Command Spectrum

```
  /gsd-fast        /gsd-quick          /gsd-do          Full Workflow
  ─────────        ──────────          ───────          ─────────────
  Fixa en typo     Lägg till feature   Beskriv vad      Helt nytt projekt
  ≤3 edits         Plan → Execute      du vill göra     Phases, roadmap,
  Ingen planning   Composable flags:   → smart routing  research, verify
                   --discuss
                   --research
                   --full

  ◄── enklare ──────────────────────────────── mer struktur ──►
```

Börja till vänster. Flytta höger när du behöver mer.
```

- Use a PLAIN code fence (triple backticks with NO language specifier) so highlight.js doesn't color the ASCII art
- Keep all lines under 75 characters wide for projector safety
- The `─` character is U+2500 (box drawing horizontal) — use it for separator lines and the arrow
- The `◄` and `►` are U+25C4 and U+25BA

**Slide 2 — /gsd-fast terminal example** (SLIDE-04, PRAC-02, per D-05/D-06/D-07/D-08):

```markdown
---

## `/gsd-fast` — noll overhead

Perfekt för triviala ändringar. Ingen planning, inga subagents.

```bash
$ /gsd-fast "add .env to .gitignore"
✅ Done: Added .env to .gitignore
   Commit: a3f2e1d
   Files: .gitignore
```

≤ 3 file edits → atomic commit → loggat i STATE.md
```

- Use `bash` language fence for syntax highlighting
- Standard `$` prompt (D-08)
- 4 lines of terminal output — within D-06 limit of 3-5
- Relatable scenario: adding .env to .gitignore (D-05)

**Slide 3 — /gsd-quick terminal example** (SLIDE-04, PRAC-03, per D-05/D-06/D-07/D-08):

```markdown
---

## `/gsd-quick` — med planning

Små tasks med GSD-struktur. Planner + executor som subagents.

```bash
$ /gsd-quick "add dark mode toggle to settings page"
Planning... 3 tasks identified
Executing... ██████████ 3/3
✅ Done — dark mode toggle added
```

Composable: `--discuss` · `--research` · `--full`
```

- Show the composable flags as inline code at the bottom, separated by middle dots
- Scenario: adding a dark mode toggle (D-05 — relatable feature work)

**Slide 4 — /gsd-do terminal example** (SLIDE-04, PRAC-03, per D-05/D-06/D-07/D-08):

```markdown
---

## `/gsd-do` — smart routing

Beskriv vad du vill göra. GSD väljer rätt kommando.

```bash
$ /gsd-do "refactor the auth system"
Routing to: /gsd-add-phase
Reason: Multi-file refactor needs full phase
```

Aldrig gör jobbet själv — router till rätt specialist.
```

- Show the routing behavior — input → analysis → handoff
- 3 lines of terminal output (compact per D-06)

**Slide 5 — Full Workflow tease** (SLIDE-04, PRAC-03, per D-05/D-06/D-07/D-08):

```markdown
---

## Full Workflow — hela maskineriet

När uppgiften kräver research, planning och verification.

```bash
$ /gsd-new-project
Vad vill du bygga? > En REST API för kundhantering
Requirements defined: 12 items
Roadmap: 5 phases created
Ready: /gsd-plan-phase 1
```

Phases · milestones · persistent memory · goal-backward verification
```

- This is a TEASE, not a full explanation — Phase 3 covers the workflow in depth
- Show the `/gsd-new-project` interactive flow
- Bottom keywords hint at capabilities without explaining them

**CRITICAL formatting notes:**
- Each slide is separated by `---` on its own line (Marp slide separator)
- Do NOT add `<!-- _class: ... -->` directives to these content slides
- Do NOT add `<!-- _paginate: skip -->` — these slides should show page numbers
- The code fences inside the markdown must be properly nested — the outer slide content is NOT itself in a code fence in the actual file
  </action>
  <verify>
    <automated>powershell -Command "
      $c = Get-Content slides.md -Raw;
      $checks = @(
        ($c -match 'Command Spectrum'),
        ($c -match '/gsd-fast.*noll overhead'),
        ($c -match '/gsd-quick.*med planning'),
        ($c -match '/gsd-do.*smart routing'),
        ($c -match 'Full Workflow.*hela maskineriet'),
        ($c -match 'enklare.*mer struktur'),
        ($c -match 'add .env to .gitignore'),
        ($c -match 'dark mode toggle'),
        ($c -match 'refactor the auth system'),
        ($c -match 'gsd-new-project')
      );
      $pass = ($checks | Where-Object { $_ }).Count;
      Write-Output \"Checks passed: $pass/10\";
      if ($pass -lt 10) { exit 1 }
    "</automated>
  </verify>
  <acceptance_criteria>
    - slides.md contains heading `## Command Spectrum` with a plain code block containing `/gsd-fast`, `/gsd-quick`, `/gsd-do`, `Full Workflow` in horizontal layout
    - slides.md contains `◄── enklare` and `mer struktur ──►` arrow in the spectrum code block
    - slides.md contains heading matching `/gsd-fast.*noll overhead` with a bash code block containing `$ /gsd-fast "add .env to .gitignore"`
    - slides.md contains heading matching `/gsd-quick.*med planning` with a bash code block containing `$ /gsd-quick`
    - slides.md contains heading matching `/gsd-do.*smart routing` with a bash code block containing `$ /gsd-do "refactor the auth system"`
    - slides.md contains heading matching `Full Workflow.*hela maskineriet` with a bash code block containing `$ /gsd-new-project`
    - All 5 new slides appear AFTER the `<!-- Section 4: The On-Ramp -->` comment
    - No new slides use `<!-- _class: divider -->` (these are content slides, not dividers)
    - Terminal examples use `$` prompt style (not `>` or `#`)
    - All ASCII art lines are under 75 characters wide
  </acceptance_criteria>
  <done>5 content slides (spectrum + 4 terminal examples) are inserted in slides.md Section 4 with correct Marp formatting, Swedish text with English terms per TERMINOLOGY.md, and all terminal examples showing relatable developer scenarios with compact 3-5 line output.</done>
</task>

<task type="auto">
  <name>Task 2: Installation slides + gateway bridge slide</name>
  <files>slides.md</files>
  <read_first>
    - slides.md (read the full file to see Task 1's inserted slides and find the correct insertion point — after the Full Workflow tease slide but before the --- leading to Section 5 divider)
    - TERMINOLOGY.md (language policy)
    - .planning/phases/02-core-payload-on-ramp/02-RESEARCH.md (installation path at lines 269-286, gateway bridge example at lines 401-417)
  </read_first>
  <action>
Insert 3 new content slides into slides.md AFTER the Full Workflow tease slide (Task 1's last slide) and BEFORE the `---` that precedes the Section 5 divider (`<!-- _class: divider -->` / `# Hela livscykeln`).

**Slide 6 — Installation prerequisites** (PRAC-01, per D-09/D-10):

```markdown
---

## Kom igång — förutsättningar

**Du behöver:**
- Node.js ≥18
- VS Code med GitHub Copilot

**Installera GSD:**

```bash
$ npx get-shit-done-cc@latest
```

Klart. Inga andra dependencies.
```

- Per D-10: Show Copilot path ONLY — do NOT mention Claude Code, OpenCode, Gemini, or Codex
- Per D-09: Step-by-step format
- Per D-12: Spacious — just the essentials, readable from back of room
- The install command is `npx get-shit-done-cc@latest` (verified from npm registry, v1.36.0)

**Slide 7 — First command** (PRAC-01, per D-09):

```markdown
---

## Ditt första kommando

```bash
$ /gsd-fast "fix the typo in README"
✅ Done — zero setup needed
```

Ingen `.planning/`-mapp behövs.
`/gsd-fast` fungerar direkt — perfekt som första smakprov.
```

- Key insight from research: `/gsd-fast` works without a project (no `.planning/` directory needed)
- This makes it the perfect "first taste" command — zero friction
- Per D-09: This is the "first command" step in the getting-started flow

**Slide 8 — Gateway bridge** (per D-14):

```markdown
---

## Men om uppgiften är större?

`/gsd-fast` fixar typon.
`/gsd-quick` lägger till en feature.

Men tänk om du ska bygga en hel REST API?
Eller refaktorera ett auth-system?

**Då behöver du hela workflow:en.**

*Nästa: Hela livscykeln →*
```

- Per D-14: Section must end with a gateway tease bridging to Section 5
- Creates curiosity and forward momentum — "we showed you the easy stuff, now let's go deeper"
- The italic "Nästa: Hela livscykeln →" matches the Section 5 divider title exactly
- Do NOT use bold for the teaser line — italic creates a softer forward pull

**CRITICAL insertion point:**
These 3 slides go AFTER the Full Workflow tease slide and BEFORE the existing `---` + Section 5 divider. The result should be:
```
[Full Workflow slide from Task 1]
---
[Slide 6: Kom igång]
---
[Slide 7: Första kommando]
---
[Slide 8: Gateway bridge]
---
<!-- _class: divider -->
<!-- _paginate: skip -->
# Hela livscykeln
```

Verify that there is exactly ONE `---` separator between the gateway bridge slide content and the Section 5 divider. Do not create a double separator.
  </action>
  <verify>
    <automated>powershell -Command "
      $c = Get-Content slides.md -Raw;
      $checks = @(
        ($c -match 'Kom igång.*förutsättningar'),
        ($c -match 'npx get-shit-done-cc@latest'),
        ($c -match 'Node\.js'),
        ($c -match 'GitHub Copilot'),
        ($c -match 'Ditt första kommando'),
        ($c -match 'Ingen \.planning'),
        ($c -match 'Men om uppgiften är större'),
        ($c -match 'Då behöver du hela workflow'),
        ($c -match 'Nästa: Hela livscykeln'),
        (-not ($c -match 'Claude Code'))
      );
      $pass = ($checks | Where-Object { $_ }).Count;
      Write-Output \"Checks passed: $pass/10\";
      if ($pass -lt 10) { exit 1 }
    "</automated>
  </verify>
  <acceptance_criteria>
    - slides.md contains heading matching `Kom igång.*förutsättningar` with prerequisites listing `Node.js` and `GitHub Copilot`
    - slides.md contains bash code block with `npx get-shit-done-cc@latest`
    - slides.md does NOT contain the string `Claude Code` anywhere in Section 4 (per D-10)
    - slides.md contains heading `Ditt första kommando` with a bash code block containing `/gsd-fast`
    - slides.md contains text `Ingen .planning` confirming zero-project-setup messaging
    - slides.md contains heading `Men om uppgiften är större?` (gateway bridge)
    - slides.md contains `Då behöver du hela workflow:en.` in bold
    - slides.md contains `Nästa: Hela livscykeln →` in italics
    - The gateway bridge slide appears BEFORE the Section 5 divider `# Hela livscykeln`
    - All 3 new slides appear AFTER the Full Workflow tease slide
    - No slide in this task uses `<!-- _class: divider -->`
  </acceptance_criteria>
  <done>3 slides (installation prerequisites, first command, gateway bridge) are inserted in slides.md Section 4 with correct Marp formatting, Copilot-only install path, zero-friction first command, and a narrative bridge to Section 5.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Visual verification of rendered slides</name>
  <files>slides.md</files>
  <what-built>8 new content slides in Section 4 "Börja enkelt": command spectrum visual (ASCII art), 4 terminal examples (/gsd-fast, /gsd-quick, /gsd-do, full workflow), installation guide (2 slides), and gateway bridge to Section 5.</what-built>
  <action>
Human visually verifies that all 8 slides render correctly in Marp preview. No automated action — this is a visual checkpoint.
  </action>
  <how-to-verify>
    1. Open slides.md in VS Code with Marp extension installed
    2. Use Marp preview (Ctrl+Shift+V or "Marp: Open Preview") to render slides
    3. Navigate to the "Börja enkelt" section divider and check the 8 slides that follow:

    **Check each slide:**
    - Slide 1 (Command Spectrum): ASCII art renders in monospace, columns aligned, arrow visible, all 4 command levels readable
    - Slide 2 (/gsd-fast): Terminal example has syntax highlighting, $ prompt visible, output compact (4 lines)
    - Slide 3 (/gsd-quick): Terminal example renders, composable flags listed at bottom
    - Slide 4 (/gsd-do): Routing example renders, 3 lines of output
    - Slide 5 (Full Workflow): Interactive prompt example renders, keyword list at bottom
    - Slide 6 (Installation): Prerequisites bullet list renders, install command in code block
    - Slide 7 (First command): /gsd-fast example renders, "Ingen .planning" messaging visible
    - Slide 8 (Gateway bridge): Bridge text renders, italic teaser visible, leads into "Hela livscykeln" divider

    **Overall checks:**
    - No blank/broken slides between content
    - Page numbers appear on all 8 content slides (not on the divider)
    - Text is readable at presentation scale (not too dense)
    - Swedish text with English terms reads naturally
    - Section flows: spectrum → examples → install → bridge (persuasion arc)
  </how-to-verify>
  <verify>Human approves visual rendering of all 8 slides in Marp preview</verify>
  <done>User has confirmed all 8 Section 4 slides render correctly with readable text, aligned ASCII art, syntax-highlighted code blocks, and natural narrative flow.</done>
  <resume-signal>Type "approved" if slides look correct, or describe specific issues (e.g., "ASCII art misaligned on slide 1", "text too dense on slide 6")</resume-signal>
</task>

</tasks>

<verification>
Phase-level verification after all tasks complete:

1. **Slide count:** slides.md Section 4 contains exactly 8 new content slides between the "Börja enkelt" divider and the "Hela livscykeln" divider
2. **Requirement coverage:**
   - SLIDE-03: Command spectrum visual exists as single ASCII art slide
   - SLIDE-04: 4 terminal examples with bash code blocks and $ prompts
   - PRAC-01: Installation steps (prerequisites + install + first command) across 2 slides
   - PRAC-02: /gsd-fast example with realistic scenario and accurate behavior
   - PRAC-03: Graduated complexity visible in spectrum + progressive examples
3. **Decision compliance:**
   - D-01/D-02/D-03/D-04: Horizontal ASCII art spectrum, single slide
   - D-05/D-06/D-07/D-08: Relatable scenarios, compact format, one per level, $ prompt
   - D-09/D-10: Step-by-step install, Copilot only
   - D-12: Spacious layout, one idea per slide
   - D-14: Gateway bridge at section end
4. **Marp rendering:** `npx @marp-team/marp-cli slides.md --html` produces valid HTML without errors
5. **Language:** All slides use Swedish text with English technical terms per TERMINOLOGY.md
</verification>

<success_criteria>
- slides.md contains 8 new content slides in Section 4 that render correctly in Marp
- Command spectrum ASCII art is readable and aligned in monospace at 0.85em
- All 4 terminal examples show realistic developer scenarios with 3-5 lines of output
- Installation path shows Copilot-only flow with npx get-shit-done-cc@latest
- Section ends with gateway bridge to "Hela livscykeln"
- Human has visually verified slide rendering and approved
</success_criteria>

<output>
After completion, create `.planning/phases/02-core-payload-on-ramp/02-01-SUMMARY.md`
</output>
