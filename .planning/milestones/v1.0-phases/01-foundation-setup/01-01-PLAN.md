---
phase: 01-foundation-setup
plan: 01
type: execute
wave: 1
depends_on: []
files_modified: [slides.md, TERMINOLOGY.md, companion.md]
autonomous: true
requirements: [SLIDE-01, SLIDE-05]

must_haves:
  truths:
    - "slides.md renders as a valid Marp slide deck without errors"
    - "Slide deck uses 16:9 aspect ratio, default theme, and page numbers only"
    - "All 8 presentation sections have divider slides in slides.md"
    - "TERMINOLOGY.md defines ≥15 GSD/tech terms with English-use / Swedish-avoid mapping"
    - "companion.md has section headers matching the presentation arc"
  artifacts:
    - path: "slides.md"
      provides: "Marp slide deck skeleton with front matter, CSS, title slide, section dividers"
      contains: "marp: true"
    - path: "TERMINOLOGY.md"
      provides: "Swedish/English terminology reference for consistent language across phases"
      contains: "Engelska (används)"
    - path: "companion.md"
      provides: "Companion document outline with section placeholders for Phase 6"
      contains: "## 1. Introduktion"
  key_links:
    - from: "slides.md"
      to: "TERMINOLOGY.md"
      via: "Language policy — all slide text in subsequent phases follows terminology reference"
      pattern: "phase|roadmap|agent|workflow"
    - from: "companion.md"
      to: "slides.md"
      via: "Section structure mirrors the 8 presentation sections"
      pattern: "Introduktion|On-Ramp|Full Workflow|Scenarion"
---

<objective>
Create the three foundation files for the GSD presentation project: a Marp slide deck skeleton with correct configuration and all section placeholders, a Swedish/English terminology reference, and a companion document outline.

Purpose: All subsequent content phases (2–5) build on this skeleton. Without it, there's no consistent structure, language policy, or file to add slides to.
Output: `slides.md`, `TERMINOLOGY.md`, `companion.md` at repo root, ready for content phases to fill.
</objective>

<execution_context>
@~/.copilot/get-shit-done/workflows/execute-plan.md
@~/.copilot/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-foundation-setup/01-CONTEXT.md
@.planning/phases/01-foundation-setup/01-RESEARCH.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create Marp slide deck skeleton (slides.md)</name>
  <files>slides.md</files>
  <read_first>
    .planning/phases/01-foundation-setup/01-RESEARCH.md
    .planning/phases/01-foundation-setup/01-CONTEXT.md
  </read_first>
  <action>
Create `slides.md` at repo root with the following exact structure:

**YAML Front Matter** (must be the very first thing in the file — no blank lines before the opening triple-dash delimiter):

    marp: true
    theme: default
    paginate: true
    size: 16:9

Wrap these 4 lines in YAML front matter delimiters (triple-dash `---` on the line before and after).
- `marp: true` — enables Marp rendering (per SLIDE-01)
- `theme: default` — per decision D-02 (no custom theme)
- `paginate: true` — per decision D-03 (page numbers only)
- `size: 16:9` — per decision D-01 (widescreen). MUST be in front matter, not HTML comment (Marp ignores `size` in comments)
- Do NOT add `footer:` or `header:` directives — per decision D-03

**Inline CSS `<style>` block** immediately after front matter:
```css
/* Section divider slides — per decision D-04 */
section.divider {
  display: flex;
  flex-direction: column;
  justify-content: center;
  align-items: center;
  text-align: center;
}
section.divider h1 {
  font-size: 2.5em;
  font-weight: bold;
  margin-bottom: 0.3em;
}
section.divider hr {
  width: 40%;
  border: none;
  border-top: 3px solid #ccc;
  margin-top: 0;
}

/* Lead slides (title slide, closing slide) */
section.lead {
  display: flex;
  flex-direction: column;
  justify-content: center;
  align-items: center;
  text-align: center;
}

/* Code blocks — terminal-style for command examples */
pre {
  font-size: 0.85em;
}
code {
  border-radius: 4px;
}
```

**Title Slide** (first content slide, after the style block):
- Apply `<!-- _class: lead -->` and `<!-- _paginate: skip -->` (local directives with underscore prefix)
- Title: `# AI Agent Frameworks — GSD`
- Subtitle: `*Hur du arbetar effektivt med AI-assisterad utveckling*`

**8 Section Divider Slides** — each followed by a content placeholder comment. Use `---` for slide separation. Each divider slide uses:
- `<!-- _class: divider -->` (local directive)
- `<!-- _paginate: skip -->` (hide page number on dividers)
- `# Section Title` (the h1)
- `<hr>` tag (NOT `---` which would create a new slide — this is a critical Marp pitfall)

The 8 sections with their exact Swedish titles and placeholder comments:

| # | Divider Title | Placeholder Comment |
|---|---------------|---------------------|
| 1 | `# Var är vi idag?` | `<!-- Section 1: The Problem — slides added by Phase 5 -->` |
| 2 | `# Vad finns?` | `<!-- Section 2: Framework Landscape — slides added by Phase 5 -->` |
| 3 | `# Hur fungerar det?` | `<!-- Section 3: GSD Mental Model — slides added by Phase 3 -->` |
| 4 | `# Börja enkelt` | `<!-- Section 4: The On-Ramp — slides added by Phase 2 -->` |
| 5 | `# Hela livscykeln` | `<!-- Section 5: Full Workflow — slides added by Phase 3 -->` |
| 6 | `# I din vardag` | `<!-- Section 6: Scenarios — slides added by Phase 4 -->` |
| 7 | `# Tänk så här` | `<!-- Section 7: Best Practices — slides added by Phase 4 -->` |
| 8 | `# Börja på måndag` | `<!-- Section 8: Getting Started — slides added by Phase 4 -->` |

After the last section placeholder, do NOT add any additional slides.

**Important Marp pitfalls to avoid:**
- No blank lines before the opening `---` of front matter (breaks rendering)
- Use `<hr>` for horizontal rules within slides, never `---` (which creates a new slide)
- Always use underscore prefix for local directives (`_class`, `_paginate`) — without underscore they apply globally to all subsequent slides
- `size: 16:9` must be in YAML front matter, not in an HTML comment
  </action>
  <verify>
    <automated>npx --yes @marp-team/marp-cli@4.3.1 slides.md --pdf -o test-output.pdf 2>&amp;1; if ($LASTEXITCODE -eq 0) { Remove-Item test-output.pdf -ErrorAction SilentlyContinue; Write-Output "MARP VALIDATION PASSED" } else { Write-Output "MARP VALIDATION FAILED" }</automated>
  </verify>
  <acceptance_criteria>
- slides.md line 1 is exactly `---` (no BOM, no blank line before it)
- slides.md contains `marp: true`
- slides.md contains `theme: default`
- slides.md contains `paginate: true`
- slides.md contains `size: 16:9`
- slides.md does NOT contain `footer:` or `header:`
- slides.md contains `section.divider {`
- slides.md contains `section.lead {`
- slides.md contains `<!-- _class: lead -->`
- slides.md contains `# AI Agent Frameworks — GSD`
- slides.md contains all 8 section divider titles: `Var är vi idag?`, `Vad finns?`, `Hur fungerar det?`, `Börja enkelt`, `Hela livscykeln`, `I din vardag`, `Tänk så här`, `Börja på måndag`
- slides.md contains 8 occurrences of `<!-- _class: divider -->`
- slides.md contains `<hr>` tags (not `---` for horizontal rules within divider slides)
- Marp CLI renders slides.md to PDF without errors (exit code 0)
  </acceptance_criteria>
  <done>slides.md exists at repo root, renders without errors in Marp CLI, has 16:9 aspect ratio, default theme, pagination, 1 title slide, and 8 section divider slides with content placeholders</done>
</task>

<task type="auto">
  <name>Task 2: Create terminology reference and companion document outline</name>
  <files>TERMINOLOGY.md, companion.md</files>
  <read_first>
    .planning/phases/01-foundation-setup/01-RESEARCH.md
    .planning/PROJECT.md
  </read_first>
  <action>
Create two files at repo root:

**File 1: `TERMINOLOGY.md`** — Swedish/English terminology reference (per SLIDE-05)

Structure:
```markdown
# Terminologi — Svenska/Engelska

## Princip

Svenska löptext, engelska facktermer. Ingen försvenskning av etablerade begrepp.
(Ref: PROJECT.md — "Inga försvenskningar av engelska begrepp")

## Termer

| Engelska (används) | Svenska (används INTE) | Kontext |
|---------------------|------------------------|---------|
```

Populate the table with these terms (minimum 15, aim for ~20):

| Engelska (används) | Svenska (används INTE) | Kontext |
|---------------------|------------------------|---------|
| phase | fas | GSD workflow stages |
| roadmap | vägkarta | Project planning |
| agent | agent | AI agent (same in both languages) |
| workflow | arbetsflöde | Process descriptions |
| orchestrator | orkestrerare | GSD architecture |
| milestone | milstolpe | Project checkpoints |
| prompt | prompt | AI interaction (same in both) |
| commit | commit | Git operations (same in both) |
| skill | förmåga | Copilot skills system |
| framework | ramverk | Tool/methodology category |
| on-ramp | — | Graduated entry concept (no Swedish equivalent) |
| brownfield | — | Existing codebase (no Swedish equivalent) |
| greenfield | — | New project (no Swedish equivalent) |
| verification | verifiering | Quality gates in GSD |
| execution | exekvering | Plan execution step |
| planning | planering | Plan creation step |
| context window | kontextfönster | LLM limitation concept |
| codebase | kodbas | Repository/project code |
| debugging | felsökning | Bug investigation |
| deploy | driftsätta | Deployment (dev term) |

Add a note section after the table:
```markdown
## Användning

- Termer i kolumnen "Engelska (används)" ska användas i alla slides och dokument
- Svenska böjningsformer är OK: "i en phase", "mellan phases", "agenten"
- Sammansättningar: använd engelska termen + svensk ändelse: "workflow-steget", "phase-strukturen"
```

**File 2: `companion.md`** — Companion reference document outline

Create with exactly this structure (section numbers and headers match the 8-section presentation arc, plus supplementary sections):

```markdown
# Arbeta effektivt med AI Agent Frameworks — GSD

> Kompletterande dokument till presentationen. Kan läsas standalone.

## 1. Introduktion
### Problemet med ostrukturerad AI-assistans
### Vad är ett agent framework?

## 2. GSD i korthet
### Arkitektur: orchestrator + agents
### .planning/ — projektets minne
### Kärnprincip: du bestämmer, GSD strukturerar

## 3. Kom igång — On-Ramp
### /gsd-fast — triviala tasks
### /gsd-do — smart routing
### /gsd-quick — tasks med planering
### Vilken ska jag välja? (beslutsträd)

## 4. Full Workflow
### /gsd-new-project — från idé till roadmap
### Phase lifecycle: plan → execute → verify
### Projektminne: STATE.md och /gsd-resume-work
### Milestones och arkivering

## 5. Scenarion
### Greenfield-projekt
### Brownfield: ny feature i befintlig kodbas
### Buggfixar och debugging
### Quick fixes och config-ändringar

## 6. Best Practices
### Tänk i outcomes
### Trust but verify
### När du INTE ska använda GSD
### Gradvis adoption

## 7. Referens
### Kommandoöversikt
### .planning/ filstruktur
### Länkar och resurser

## Appendix
### GSD vs BMAD — kortjämförelse
```

Each section heading should be present but with NO content below it — just the headers. Content phases (2–6) will fill these in. Do NOT add placeholder text like "TBD" or "Coming soon" — empty sections are intentional and cleaner for subsequent phases to fill.
  </action>
  <verify>
    <automated>(Test-Path TERMINOLOGY.md) -and (Test-Path companion.md) -and ((Select-String -Path TERMINOLOGY.md -Pattern "phase|roadmap|agent|workflow|orchestrator|milestone|prompt|commit|skill|framework|on-ramp|brownfield|greenfield|verification|execution" | Measure-Object).Count -ge 15) -and ((Select-String -Path companion.md -Pattern "^## " | Measure-Object).Count -ge 7)</automated>
  </verify>
  <acceptance_criteria>
- TERMINOLOGY.md exists at repo root
- TERMINOLOGY.md contains `# Terminologi — Svenska/Engelska`
- TERMINOLOGY.md contains `Engelska (används)` table header
- TERMINOLOGY.md contains `Svenska (används INTE)` table header
- TERMINOLOGY.md contains at least 15 rows in the term table (terms: phase, roadmap, agent, workflow, orchestrator, milestone, prompt, commit, skill, framework, on-ramp, brownfield, greenfield, verification, execution)
- TERMINOLOGY.md contains `## Användning` section with usage guidance
- companion.md exists at repo root
- companion.md contains `# Arbeta effektivt med AI Agent Frameworks — GSD`
- companion.md contains `## 1. Introduktion`
- companion.md contains `## 2. GSD i korthet`
- companion.md contains `## 3. Kom igång — On-Ramp`
- companion.md contains `## 4. Full Workflow`
- companion.md contains `## 5. Scenarion`
- companion.md contains `## 6. Best Practices`
- companion.md contains `## 7. Referens`
- companion.md contains `## Appendix`
  </acceptance_criteria>
  <done>TERMINOLOGY.md has ≥15 English/Swedish term mappings with usage guidance; companion.md has all 7 numbered sections plus appendix matching the presentation arc; both files exist at repo root</done>
</task>

</tasks>

<verification>
Phase 1 is complete when:

1. **Marp rendering** — `npx @marp-team/marp-cli@4.3.1 slides.md --pdf -o test.pdf` exits with code 0
2. **Slide structure** — `Select-String -Path slides.md -Pattern "_class: divider"` returns 8 matches
3. **Front matter** — `Select-String -Path slides.md -Pattern "marp: true|theme: default|paginate: true|size: 16:9"` returns 4 matches
4. **No forbidden directives** — `Select-String -Path slides.md -Pattern "^footer:|^header:"` returns 0 matches
5. **Term coverage** — `Select-String -Path TERMINOLOGY.md -Pattern "^\|"` returns ≥16 lines (header + 15 terms)
6. **Companion structure** — `Select-String -Path companion.md -Pattern "^## [0-9]"` returns 7 matches (sections 1-7)
7. **All 3 files exist** — `(Test-Path slides.md) -and (Test-Path TERMINOLOGY.md) -and (Test-Path companion.md)` returns True
</verification>

<success_criteria>
- slides.md renders as a valid Marp deck in 16:9 with default theme and pagination
- All 8 section dividers are present with correct Swedish titles and proper CSS class application
- Title slide has presentation title and subtitle
- TERMINOLOGY.md has ≥15 terms with clear English-use / Swedish-avoid mapping
- companion.md outline mirrors the presentation's 8-section arc
- No content beyond skeleton/placeholders — subsequent phases fill in content
</success_criteria>

<output>
After completion, create `.planning/phases/01-foundation-setup/01-01-SUMMARY.md`
</output>
