---
phase: 04-scenarios-best-practices
plan: "01"
type: execute
wave: 1
depends_on: []
files_modified: [slides.md]
autonomous: false
requirements: [SCEN-01, SCEN-02, SCEN-03, SCEN-04, PRAC-04]

must_haves:
  truths:
    - "Developer can look at decision tree and identify the right GSD command for their situation"
    - "Greenfield scenario shows /gsd-new-project as full workflow entry point"
    - "Brownfield scenario introduces /gsd-map-codebase as the distinct brownfield entry point"
    - "Quick fix/debugging scenario shows two distinct paths: /gsd-fast for trivial and /gsd-debug for investigation"
    - "Decision tree content is entirely in English (exception to Swedish deck pattern)"
  artifacts:
    - path: "slides.md"
      provides: "Section 6 content slides between divider and Section 7"
      contains: "gsd-map-codebase"
    - path: "slides.md"
      provides: "Decision tree ASCII art with 8+ command endpoints"
      contains: "gsd-new-project"
  key_links:
    - from: "Section 6 decision tree"
      to: "Commands introduced in Sections 4-5"
      via: "command name references — audience already knows these commands"
      pattern: "gsd-new-project|gsd-quick|gsd-fast|gsd-debug|gsd-map-codebase|gsd-add-phase|gsd-do|gsd-resume-work"
    - from: "Brownfield scenario slides"
      to: "/gsd-map-codebase workflow"
      via: "terminal example showing codebase mapping output"
      pattern: "gsd-map-codebase"
---

<objective>
Insert Section 6 "I din vardag" content slides into the Marp deck: a decision tree mapping developer situations to GSD commands (the "slide people photograph"), plus three real-world scenario mini-walkthroughs (greenfield, brownfield, quick fix/debugging).

Purpose: After learning what GSD is (Section 3), how to start (Section 4), and the full workflow (Section 5), Section 6 answers "How do I actually use this at work?" by mapping common developer situations to the right GSD command.
Output: ~6 new content slides in slides.md Section 6
</objective>

<execution_context>
@~/.copilot/get-shit-done/workflows/execute-plan.md
@~/.copilot/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/04-scenarios-best-practices/04-CONTEXT.md
@.planning/phases/04-scenarios-best-practices/04-RESEARCH.md
@TERMINOLOGY.md
@slides.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Insert Section 6 decision tree and scenario slides</name>
  <files>slides.md</files>
  <read_first>
    - slides.md (find current Section 6 insertion point — the comment `<!-- Section 6: Scenarios — slides added by Phase 4 -->` and the `---` separator before Section 7 divider)
    - TERMINOLOGY.md (Swedish text policy — but decision tree content is ALL-ENGLISH per D-01)
    - .planning/phases/04-scenarios-best-practices/04-RESEARCH.md (lines 503-549 have exact slide examples; lines 174-213 have verified command inventory)
  </read_first>
  <action>
Insert ~6 new content slides between the `<!-- Section 6: Scenarios — slides added by Phase 4 -->` comment and the `---` separator that precedes the Section 7 divider (`<!-- _class: divider -->` for "Tänk så här"). Each new slide starts with `---`. Follow established patterns: no `_class` directive on content slides, no `_paginate: skip`, Swedish text with English technical terms (EXCEPT the decision tree code block which is ALL-ENGLISH per D-01).

**Slide 1: Decision tree (SCEN-04, D-01, D-02, D-03)**

Title: `## Vilket kommando ska jag använda?`

Body: A plain code fence (no language specifier — prevents syntax highlighting, same as Phase 2 ASCII art). Inside, an all-English Q&A branching list using box-drawing characters (├── │ └──). Must cover these 8 commands as leaf nodes:
- `/gsd-new-project` (whole project)
- `/gsd-quick --discuss` (one feature)
- `/gsd-map-codebase` (first time in existing code)
- `/gsd-debug` (bug or error)
- `/gsd-add-phase` (big refactor)
- `/gsd-fast` (trivial fix)
- `/gsd-do` (not sure which command)
- `/gsd-resume-work` (resuming after break)

Structure the tree as 4 top-level branches: "Starting something new?", "Working on existing code?", "Something small?", "Coming back?". Keep all lines under 75 characters. Example from research:

```
What are you doing?
├── Starting something new?
│   ├── Whole project       → /gsd-new-project
│   └── One feature         → /gsd-quick --discuss
├── Working on existing code?
│   ├── First time here?    → /gsd-map-codebase
│   ├── Bug or error?       → /gsd-debug
│   └── Big refactor?       → /gsd-add-phase
├── Something small?
│   ├── Trivial fix (≤3)    → /gsd-fast
│   └── Not sure?           → /gsd-do
└── Coming back?
    └── Resuming work       → /gsd-resume-work
```

**Slide 2: Greenfield scenario (SCEN-01)**

Title: `## Greenfield — nytt projekt`

Body: A bash code fence showing a compressed `/gsd-new-project` terminal walkthrough. Show the flow from idea → structured output. Example content:

```bash
$ /gsd-new-project
Vad vill du bygga? → "En REST API för kundhantering"
✅ 12 requirements defined
✅ 5 phases created
✅ Phase 1 planned and executed
✅ "Kan en användare registrera sig? ✅"
```

Below the code block, a one-line summary: `Från idé till verifierat resultat — utan att lämna editorn.`

Do NOT re-explain `/gsd-new-project` mechanics (Section 5 already did that). Frame this as APPLICATION, not explanation.

**Slide 3: Brownfield step 1 — mapping (SCEN-02, PRAC-04)**

Title: `## Brownfield — kartlägg först`

Body: Explain that for existing codebases, start with `/gsd-map-codebase`. Show a bash terminal example:

```bash
$ /gsd-map-codebase
Mapping... 7 analysis documents created
   .planning/codebase/ARCHITECTURE.md
   .planning/codebase/CONVENTIONS.md
   .planning/codebase/STRUCTURE.md
   ...
```

Below the code block: `GSD förstår nu din kodbas — mönster, konventioner, beroenden.`

This slide establishes `/gsd-map-codebase` as THE brownfield entry point — the key differentiator that gives GSD project memory about code you didn't write.

**Slide 4: Brownfield step 2 — adding features (SCEN-02)**

Title: `## Brownfield — planera som vanligt`

Body: After mapping, show the next step — adding a feature or phase using the mapped context:

```bash
$ /gsd-add-phase "add search endpoint to user API"
Research uses codebase map → respects existing patterns
✅ Phase planned with 2 plans
```

Below the code block: `Kartan gör skillnaden — utan den gissar agenten om din projektstruktur.`

This reinforces why the mapping step matters: plans reference the codebase analysis docs.

**Slide 5: Quick fix / debugging (SCEN-03)**

Title: `## Quick fix — två vägar`

Body: Show two distinct paths side by side or sequentially:

Path A heading (bold text): **Enkelt? `/gsd-fast`**
```bash
$ /gsd-fast "fix the 401 error in login validation"
✅ Done: Fixed token expiry check
   Files: src/auth.ts
```

Path B heading (bold text): **Behöver utredning? `/gsd-debug`**
```bash
$ /gsd-debug
Parallel agents investigating...
✅ Root cause: missing dependency array in useEffect
   Created: DEBUG-login-401.md
```

Below: `Börja med `/gsd-fast`. Är problemet djupare — `/gsd-debug` utreder systematiskt.`

**Slide 6: Bridge to Section 7**

A brief transition slide. Title: `## Rätt kommando är halva jobbet`

Body (2-3 lines of text, no code block):

`Ni har verktygen. Men agentens resultat beror på hur ni tänker och kommunicerar.`

`Nästa steg: mindset shifts och best practices.`

This bridges from "which command" to "how to think" — transitioning from Section 6 into Section 7 "Tänk så här".
  </action>
  <verify>
    <automated>cd C:\source\repos\docs; Select-String -Pattern "gsd-map-codebase" slides.md; Select-String -Pattern "gsd-new-project" slides.md; Select-String -Pattern "gsd-debug" slides.md; Select-String -Pattern "gsd-fast" slides.md; Select-String -Pattern "gsd-resume-work" slides.md; Select-String -Pattern "gsd-add-phase" slides.md; Select-String -Pattern "What are you doing" slides.md</automated>
  </verify>
  <acceptance_criteria>
    - slides.md contains `What are you doing?` inside a code block (decision tree is all-English per D-01)
    - slides.md contains all 8 command references: `gsd-new-project`, `gsd-quick`, `gsd-map-codebase`, `gsd-debug`, `gsd-add-phase`, `gsd-fast`, `gsd-do`, `gsd-resume-work` in Section 6
    - slides.md contains `## Vilket kommando ska jag använda?` as decision tree slide title
    - slides.md contains `## Greenfield` as a slide title in Section 6
    - slides.md contains `## Brownfield` as slide title(s) in Section 6
    - slides.md contains `## Quick fix` as a slide title in Section 6
    - No `<!-- _class:` directives on any content slide (only dividers use those)
    - No Swedish words inside the decision tree code block (D-01 — all-English inside fence)
    - Terminal examples use bash code fence with `$` prompt prefix (per D-14)
    - ASCII decision tree uses plain code fence with no language specifier (per D-15)
    - New slides inserted AFTER `<!-- Section 6: Scenarios — slides added by Phase 4 -->` comment
    - New slides inserted BEFORE the `---` separator preceding Section 7 divider
    - Each new slide separated by `---`
  </acceptance_criteria>
  <done>6 content slides exist in Section 6: decision tree with 8 command endpoints (all-English), greenfield scenario (1 slide), brownfield scenario (2 slides), quick fix/debugging (1 slide), and bridge to Section 7 (1 slide). All commands from the verified GSD inventory appear in the decision tree.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Visual verification of Section 6 slides</name>
  <files>slides.md</files>
  <action>
Human visual verification of 6 content slides in Section 6 "I din vardag": decision tree mapping developer situations to GSD commands, greenfield/brownfield/debugging scenarios with terminal examples, and bridge to Section 7.

Steps:
1. Open slides.md in VS Code with Marp extension preview (or run `npx @marp-team/marp-cli slides.md --html -o test-output.html` and open test-output.html)
2. Navigate to Section 6 "I din vardag" divider
3. Verify decision tree slide:
   - Title is in Swedish, tree content is ALL-ENGLISH (no Swedish words inside the code block)
   - Tree is readable at slide size (lines under 75 chars)
   - All 8 commands present as leaf nodes
   - Box-drawing characters render correctly
4. Verify scenario slides:
   - Greenfield shows compressed /gsd-new-project flow
   - Brownfield has 2 slides: mapping (/gsd-map-codebase) then adding features (/gsd-add-phase)
   - Quick fix shows two paths: /gsd-fast and /gsd-debug
5. Verify terminal examples match established Phase 2-3 style (bash fence, $ prompt, compact output)
6. Verify spacious layout — one idea per slide (D-12)
7. Verify bridge slide transitions to "Tänk så här"

Resume signal: Type "approved" or describe issues to fix
  </action>
  <verify>Human approval of visual output</verify>
  <done>User has visually confirmed all 6 Section 6 slides render correctly, decision tree is readable and all-English, scenarios show distinct workflows, and terminal examples match established style.</done>
</task>

</tasks>

<verification>
- All 8 GSD commands from verified inventory appear in the decision tree
- Decision tree content is all-English (D-01)
- Brownfield scenario prominently features /gsd-map-codebase (PRAC-04)
- Each scenario shows a distinct GSD entry point — no repetition with Sections 4-5
- Terminal examples follow established bash fence + $ prompt pattern
- Slides follow one-idea-per-slide spacious layout (D-12)
</verification>

<success_criteria>
Section 6 has ~6 content slides covering the decision tree and 3 scenarios. A developer viewing the decision tree can identify the right GSD command for greenfield, brownfield, quick fix, and resumption situations. The brownfield scenario clearly introduces /gsd-map-codebase as a distinct entry point. Terminal examples are visually consistent with Sections 4-5.
</success_criteria>

<output>
After completion, create `.planning/phases/04-scenarios-best-practices/04-01-SUMMARY.md`
</output>
