---
phase: "03-core-payload-full-workflow"
plan: "02"
type: execute
wave: 2
depends_on:
  - "03-01"
files_modified:
  - "slides.md"
autonomous: false
requirements:
  - "DEEP-01"
  - "DEEP-03"
  - "DEEP-04"
  - "DEEP-05"
  - "DEEP-06"
  - "PRAC-08"
must_haves:
  truths:
    - "Section 5 shows the 5-step lifecycle pipeline (new-project → discuss → plan → execute → verify) as ASCII art"
    - "Section 5 contains /gsd-new-project terminal example with interactive questioning flow"
    - "Section 5 shows realistic ROADMAP.md excerpt with success criteria"
    - "Section 5 shows composable flags (--discuss, --research, --full) with when-to-use guidance"
    - "Section 5 demonstrates persistent memory with STATE.md excerpt and /gsd-resume-work terminal example"
    - "Section 5 mentions project lifecycle management commands (start, pause, resume, cleanup)"
    - "Section 5 has verification contrast showing task completion ≠ goal achievement as the intellectual climax"
    - "Section 5 explains goal-backward verification with the 3-question methodology"
  artifacts:
    - path: "slides.md"
      provides: "8 new content slides in Section 5 between Section 5 divider and Section 6 divider"
      contains: "Task completion"
  key_links:
    - from: "Section 4 gateway bridge (Men om uppgiften är större?)"
      to: "Section 5 first content slide (lifecycle overview)"
      via: "narrative fulfillment — full workflow after gateway bridge promise"
      pattern: "Fem steg"
    - from: "Section 5 verification slides"
      to: "Section 6 divider (I din vardag)"
      via: "intellectual climax leads into practical scenarios"
      pattern: "Goal-backward"
---

<objective>
Insert 8 slides into Section 5 ("Hela livscykeln") of the Marp slide deck, walking through the complete GSD lifecycle: the 5-step workflow pipeline, /gsd-new-project deep-dive, planning artifacts, composable flags, persistent memory, project lifecycle management, and the verification philosophy climax ("task completion ≠ goal achievement").

Purpose: Fulfill the gateway bridge promise from Section 4 ("Då behöver du hela workflow:en"). The audience leaves understanding GSD's full machinery — not just quick commands, but the complete lifecycle for serious projects.

Output: 8 new content slides in slides.md Section 5.
</objective>

<execution_context>
@~/.copilot/get-shit-done/workflows/execute-plan.md
@~/.copilot/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-core-payload-full-workflow/03-RESEARCH.md
@.planning/phases/03-core-payload-full-workflow/03-CONTEXT.md
@.planning/phases/03-core-payload-full-workflow/03-01-SUMMARY.md
@TERMINOLOGY.md
@slides.md

<interfaces>
<!-- Key patterns from Phase 1/2/Plan-01 that executor must follow -->

From slides.md (after Plan 01 execution):
- Section 3 now has 4 content slides (architecture, waves, .planning/) before the "Börja enkelt" divider
- Section 4 has 8 content slides from Phase 2 ending with gateway bridge "Men om uppgiften är större?"
- Section 5 insertion point: after comment <!-- Section 5: Full Workflow — slides added by Phase 3 -->
- Section 6 divider follows: <!-- _class: divider --> / # I din vardag
- Content slides: ## title, NO _class directives, --- separators
- Terminal examples: ```bash fence, $ prompt, 3-5 lines output
- ASCII art: plain code fence (no language specifier)
- File excerpts: ```markdown fence for .md file content

From TERMINOLOGY.md:
- English terms: phase, workflow, agent, orchestrator, execution, verification, planning, milestone, codebase
- Swedish grammar: "i en phase", "workflow-steget", "phase-strukturen"
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Insert Section 5 full lifecycle slides (8 slides)</name>
  <files>slides.md</files>
  <read_first>
    - slides.md (find Section 5 insertion point at `<!-- Section 5: Full Workflow — slides added by Phase 3 -->` — note: line numbers have shifted after Plan 01 added Section 3 slides. Find by comment text, not line number.)
    - .planning/phases/03-core-payload-full-workflow/03-RESEARCH.md (lifecycle pipeline ASCII art around line 464, terminal examples around line 531, ROADMAP excerpt around line 497, STATE excerpt around line 514, verification contrast around line 543, composable flags table around line 252, project lifecycle commands around line 350)
    - TERMINOLOGY.md (language policy)
    - .planning/phases/02-core-payload-on-ramp/02-01-SUMMARY.md (established patterns: bash fence for terminal, plain fence for ASCII art)
  </read_first>
  <action>
Insert 8 new content slides into slides.md between the Section 5 comment and the Section 6 divider. The edit replaces the transition from the Section 5 comment to the Section 6 divider, inserting slides in between while preserving the Section 6 divider intact.

**Edit operation:** Replace the block from `<!-- Section 5: Full Workflow — slides added by Phase 3 -->` through the `---` separator and up to (but not including) `<!-- _class: divider -->` for "I din vardag". Insert the 8 slides after the comment, each separated by `---`, with a final `---` before the Section 6 divider.

**Formatting rules (same as Plan 01 / Phase 2):**
- Each slide separated by `---` on its own line
- Content slides use `##` titles, NO `<!-- _class: ... -->` directives
- Terminal examples in `bash` code fence, file excerpts in `markdown` code fence, ASCII art in plain code fence
- Swedish text with English technical terms per TERMINOLOGY.md
- One idea per slide (per D-12)

**Slide 1 — "Fem steg — från idé till verifierat resultat" (DEEP-01 primary):**

```markdown
---

## Fem steg — från idé till verifierat resultat

```
  /gsd-new-project
       │
       ▼
  ┌─────────┐   ┌─────────┐   ┌─────────┐   ┌─────────┐
  │ Discuss  │──▶│  Plan   │──▶│ Execute │──▶│ Verify  │
  │ Beslut   │   │Research │   │  Waves  │   │ Mål ≠   │
  │ & val    │   │ + plan  │   │  + code │   │ tasks   │
  └─────────┘   └─────────┘   └─────────┘   └─────────┘
       │              │              │              │
  CONTEXT.md    PLAN.md +      SUMMARY.md    VERIFICATION.md
                RESEARCH.md
```

Varje steg producerar artifacts → nästa steg läser dem.
```

- Plain code fence (NO language specifier) for the pipeline ASCII art
- Show the 5 steps: new-project → discuss → plan → execute → verify
- Bottom row shows artifacts each step produces — reinforces the .planning/ structure from Section 3
- Arrow characters: │ ▼ ──▶ (U+2502, U+25BC, U+2500 + U+25B6)
- Footer line emphasizes the chain: each step feeds the next

**Slide 2 — "`/gsd-new-project` — det börjar här" (DEEP-01 deep-dive):**

```markdown
---

## `/gsd-new-project` — det börjar här

```bash
$ /gsd-new-project
Vad vill du bygga? > En REST API för kundhantering
...
Requirements defined: 12 items
Roadmap: 5 phases created
Ready: /gsd-plan-phase 1
```

Djup-intervju → research → requirements → roadmap.
Allt sparas i `.planning/` — redo för nästa steg.
```

- Use `bash` code fence for terminal example
- Show the interactive flow — user input after `>`
- Ellipsis `...` indicates abbreviated questioning phase
- Footer describes the 4-step initialization process
- This references Phase 2's Full Workflow tease slide (which showed the same command) but goes deeper with the two-line footer explanation

**Slide 3 — "Artifacts — vad som faktiskt sparas" (DEEP-03 detail):**

```markdown
---

## Artifacts — vad som faktiskt sparas

```markdown
## Phases

- [x] Phase 1: Foundation
- [ ] **Phase 2: Auth** ← du är här
- [ ] Phase 3: API endpoints

### Phase 2: Auth
**Goal**: Användare kan logga in
**Success Criteria:**
  1. Login-formulär validerar credentials
  2. JWT-tokens utfärdas vid lyckad inloggning
  3. Skyddade routes avvisar oautentiserade requests
```

Inte TODO-listor — *success criteria* som kan verifieras.
```

- Use `markdown` code fence for the ROADMAP.md excerpt
- Use a generic REST API project scenario (per research recommendation: relatable, not meta)
- Show phase checkboxes with `[x]` and `[ ]` to illustrate progress tracking
- Arrow `← du är här` shows current position
- Success criteria format demonstrates verifiable goals (not vague "make auth work")
- Footer contrasts with TODO-list mentality — emphasizes verifiability

**Slide 4 — "Composable flags — anpassa assistansnivån" (DEEP-06, per D-02: reference Phase 2):**

```markdown
---

## Composable flags — anpassa assistansnivån

Kom ihåg `/gsd-quick` från förra sektionen?

| Flag | Lägger till | När |
|------|-------------|-----|
| `--discuss` | Diskussion före planning | "Jag vet inte alla detaljer" |
| `--research` | Research-agent undersöker | "Jag vet inte bästa sättet" |
| `--full` | Plan-checking + verification | "Det här måste bli rätt" |

Kombinera fritt: `/gsd-quick --discuss --research --full`
```

- Reference back to Phase 2's /gsd-quick slide ("Kom ihåg...") to avoid repetition (per D-02)
- Markdown table with 3 rows — one per flag
- "När" column uses quoted Swedish phrases showing the developer's internal monologue
- Footer shows that flags compose: all three can be used together
- Do NOT repeat the /gsd-quick terminal example from Phase 2

**Slide 5 — "GSD glömmer aldrig" (DEEP-04 primary, per D-08: session break scenario):**

```markdown
---

## GSD glömmer aldrig

```markdown
## Current Position

Phase: 2 of 5
Plan: 1 of 2
Status: Executing

Progress: [████░░░░░░] 35%

## Session Continuity

Stopped at: JWT middleware implementation
Resume: /gsd-resume-work
```

*3 dagar senare...*

```bash
$ /gsd-resume-work
📍 Phase 2: Auth System — Plan 1, Task 3
   Stopped at: JWT middleware
   Kvar: 2 tasks i Plan 1, sedan Plan 2
```
```

- This slide has TWO code blocks — a `markdown` fence showing STATE.md content, then a `bash` fence showing /gsd-resume-work output
- The italic "*3 dagar senare...*" between the blocks creates the session break narrative
- The contrast (what's stored vs what's restored) demonstrates persistent memory viscerally
- Progress bar uses Unicode block characters: █ (U+2588) and ░ (U+2591)
- Emoji: 📍 (U+1F4CD) for location pin

**Slide 6 — "Hela projektets livscykel" (PRAC-08, per D-09: minimal, one slide max):**

```markdown
---

## Hela projektets livscykel

**Starta:** `/gsd-new-project` — skapar allt från scratch

**Pausa & återuppta:** `/gsd-pause-work` → `/gsd-resume-work`

**Kolla status:** `/gsd-progress` — var står projektet?

**Städa upp:** `/gsd-cleanup` — arkivera klara phases

**Byt projekt:** Workspaces isolerar olika projekt
```

- Per D-09: One slide maximum, capability awareness only
- Bold labels + inline code commands + short Swedish descriptions
- No terminal examples — just capability listing
- 5 lifecycle capabilities: start, pause/resume, status, cleanup, workspace switching
- Uses actual GSD command names (verifiable against research)

**Slide 7 — "Task completion ≠ Goal achievement" (DEEP-05 primary, per D-10: contrast example as climax):**

```markdown
---

## Task completion ≠ Goal achievement

**Task-checking (farligt):**

```
✅ LoginComponent.tsx skapad
✅ jwt-utils.ts skapad
✅ RouteGuard.tsx skapad
────────────────────────
3/3 tasks klara! 🎉
```

**Goal-backward (GSD):**

```
Goal: "Användare kan logga in"
  ✅ Exists: Alla filer finns
  ❌ Substantive: LoginComponent returnerar <div/>
  ❌ Wired: RouteGuard importeras inte
  ❌ Functional: Inloggning fungerar inte
────────────────────────
Tasks klara — men målet INTE uppnått.
```
```

- This is the INTELLECTUAL CLIMAX of the entire Section 5 — make it the most memorable slide
- Two code blocks with plain code fences (no language specifier) — the contrast IS the point
- First block: green checkmarks create false confidence (task-level checking passes)
- Second block: red X marks reveal the reality (goal-backward catches stub implementations)
- Shows 3 of the 4 verification levels explicitly: Exists ✅, Substantive ❌, Wired ❌, Functional ❌
- The last line "Tasks klara — men målet INTE uppnått." is the key takeaway
- Emoji: ✅ (U+2705), ❌ (U+274C), 🎉 (U+1F389)
- Do NOT add explanation text around the code blocks — let the contrast speak for itself. The bold labels "Task-checking (farligt):" and "Goal-backward (GSD):" provide enough framing.

**Slide 8 — "Goal-backward verification" (DEEP-05 methodology, per D-11: 2 levels + brief 4-level mention):**

```markdown
---

## Goal-backward verification

GSD frågar tre saker:

1. Vad måste vara **SANT** för att målet ska vara uppnått?
2. Vad måste **FINNAS** för att det ska vara sant?
3. Vad måste vara **KOPPLAT** för att det ska fungera?

Sedan verifierar den i fyra nivåer:
**Exists** → **Substantive** → **Wired** → **Functional**

> Inte "är koden skriven?" utan "fungerar det för användaren?"
```

- Three numbered questions make the methodology actionable (not just philosophy)
- Bold keywords (SANT, FINNAS, KOPPLAT) create visual anchors
- One-line summary of the 4 verification levels — enough to understand without deep-diving (per D-11)
- Arrow chain shows progression: → between each level
- Blockquote footer restates the philosophical shift in a memorable one-liner
- This slide complements Slide 7 (the example) with the methodology (the framework)

**CRITICAL post-insertion checks:**
1. The Section 6 divider (`<!-- _class: divider -->` + `# I din vardag`) is still intact after the new slides
2. There is exactly one `---` between the last new slide and the Section 6 divider
3. No `<!-- _class: ... -->` appears on any of the 8 new content slides
4. The Section 5 comment is preserved (don't delete it)
5. The Section 5 divider slide (`# Hela livscykeln`) is NOT modified — it stays as the section opener
  </action>
  <verify>
    <automated>powershell -Command "$c = Get-Content slides.md -Raw; $fail = @(); if ($c -notmatch 'Fem steg') { $fail += 'DEEP-01: Missing lifecycle overview' }; if ($c -notmatch 'gsd-new-project.*det börjar här') { $fail += 'DEEP-01: Missing new-project slide' }; if ($c -notmatch 'Artifacts.*faktiskt sparas') { $fail += 'DEEP-03: Missing artifacts slide' }; if ($c -notmatch 'Composable flags.*assistansnivån') { $fail += 'DEEP-06: Missing flags slide' }; if ($c -notmatch 'glömmer aldrig') { $fail += 'DEEP-04: Missing memory slide' }; if ($c -notmatch 'livscykel') { $fail += 'PRAC-08: Missing lifecycle slide' }; if ($c -notmatch 'Task completion') { $fail += 'DEEP-05: Missing verification contrast' }; if ($c -notmatch 'Goal-backward verification') { $fail += 'DEEP-05: Missing goal-backward slide' }; if ($c -notmatch 'I din vardag') { $fail += 'Section 6 divider broken' }; if ($fail.Count -eq 0) { Write-Output 'PASS: All 8 Section 5 slides present and Section 6 intact' } else { $fail | ForEach-Object { Write-Output \"FAIL: $_\" }; exit 1 }"</automated>
  </verify>
  <acceptance_criteria>
    - slides.md contains `## Fem steg` with ASCII art pipeline showing Discuss → Plan → Execute → Verify
    - slides.md contains `/gsd-new-project` heading with bash terminal example showing interactive flow
    - slides.md contains `## Artifacts` with a markdown-fenced ROADMAP.md excerpt containing `Success Criteria`
    - slides.md contains `## Composable flags` with a markdown table containing `--discuss`, `--research`, `--full`
    - slides.md contains `## GSD glömmer aldrig` with both a markdown-fenced STATE.md excerpt AND a bash-fenced /gsd-resume-work terminal example
    - slides.md contains `## Hela projektets livscykel` with `/gsd-new-project`, `/gsd-pause-work`, `/gsd-resume-work`, `/gsd-progress`, `/gsd-cleanup` mentioned
    - slides.md contains `## Task completion` with two plain code blocks contrasting task-checking (✅ only) vs goal-backward (✅ and ❌)
    - slides.md contains `## Goal-backward verification` with 3 numbered questions and the 4-level chain `Exists → Substantive → Wired → Functional`
    - All 8 slides use `##` titles with no `<!-- _class: ... -->` directives
    - Section 6 divider `# I din vardag` remains intact after the new slides
    - Section 5 comment `<!-- Section 5: Full Workflow — slides added by Phase 3 -->` is preserved
    - No duplicate `---` separators (no blank slides)
  </acceptance_criteria>
  <done>8 content slides inserted in Section 5 of slides.md covering the complete GSD lifecycle: pipeline overview, /gsd-new-project deep-dive, ROADMAP artifacts, composable flags, persistent memory, project lifecycle, verification philosophy (task ≠ goal), and goal-backward methodology. Section 6 divider and all existing content remain intact.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Visual verification of all Phase 3 slides</name>
  <files>slides.md</files>
  <what-built>12 new content slides across Section 3 (4 slides: architecture, waves, .planning/) and Section 5 (8 slides: lifecycle, artifacts, flags, memory, verification). These are the core intellectual payload of the presentation.</what-built>
  <action>
Human visually verifies that all 12 Phase 3 slides render correctly in Marp preview. No automated action — this is a visual checkpoint.
  </action>
  <how-to-verify>
    1. Open slides.md in VS Code with Marp extension installed
    2. Use Marp preview (Ctrl+Shift+V or "Marp: Open Preview") to render slides
    3. Navigate through ALL slides checking:

    **Section 3 ("Hur fungerar det?") — 4 slides after divider:**
    - Slide "GSD — tre byggstenar": Three bold items readable, blockquote visible
    - Slide "18 agents, 4 roller": ASCII art box diagram renders in monospace, columns aligned, all 4 categories visible
    - Slide "Wave-baserad execution": Wave diagram readable, Wave 1 shows two parallel plans, Wave 2 shows dependent plan
    - Slide ".planning/ — projektets minne": File tree renders with tree characters (├ └ │), annotations after ← arrows readable

    **Section 4 ("Börja enkelt") — 8 existing slides (should be unchanged):**
    - Quick scan: Command spectrum, 4 terminal examples, install, first command, gateway bridge — all intact

    **Section 5 ("Hela livscykeln") — 8 slides after divider:**
    - Slide "Fem steg": Pipeline ASCII art renders, 4 boxes connected with arrows, artifact names below
    - Slide "/gsd-new-project": Terminal example with bash highlighting, interactive prompt visible
    - Slide "Artifacts": ROADMAP.md excerpt with markdown syntax highlighting, checkboxes (☑/☐) visible
    - Slide "Composable flags": Table renders with 3 rows, "Kom ihåg" reference to Phase 2 visible
    - Slide "GSD glömmer aldrig": TWO code blocks visible — STATE.md excerpt then terminal. "3 dagar senare..." italic text between them
    - Slide "Hela projektets livscykel": 5 bullet-like items with bold labels and inline code commands
    - Slide "Task completion ≠ Goal achievement": TWO contrast blocks — green checks vs red X marks. This should be the most visually striking slide.
    - Slide "Goal-backward verification": 3 numbered questions with bold keywords, 4-level chain, blockquote footer

    **Overall checks:**
    - No blank/broken slides between content
    - Page numbers appear on all content slides (not on dividers)
    - Text is readable at presentation scale (not too dense, especially slides with two code blocks)
    - Swedish text with English terms reads naturally throughout
    - Narrative flow: mental model → easy commands → full workflow feels like chapters, not disconnected sections
    - The gateway bridge "Men om uppgiften är större?" → Section 5 opener feels like a fulfilled promise
    - The verification slides (Slides 7-8 of Section 5) feel like the intellectual climax
  </how-to-verify>
  <verify>Human approves visual rendering of all 12 Phase 3 slides across Sections 3 and 5 in Marp preview</verify>
  <acceptance_criteria>
    - Human confirms all 12 Phase 3 slides render correctly in Marp preview
    - Human confirms ASCII art diagrams display in monospace with aligned columns
    - Human confirms text density is appropriate (readable from back of room)
    - Human confirms narrative flow across Section 3 → 4 → 5 feels coherent
  </acceptance_criteria>
  <done>User has confirmed all 12 Phase 3 slides (4 in Section 3, 8 in Section 5) render correctly with readable ASCII art, proper code highlighting, natural Swedish/English text, and coherent narrative flow across sections.</done>
  <resume-signal>Type "approved" if slides look correct, or describe specific issues (e.g., "ASCII art misaligned on agent diagram", "verification contrast slide too dense", "STATE.md excerpt unreadable")</resume-signal>
</task>

</tasks>

<verification>
Phase-level verification after all tasks complete:

```powershell
$c = Get-Content slides.md -Raw
$checks = @{
  'DEEP-01 lifecycle' = ($c -match 'Fem steg' -and $c -match 'Discuss.*Plan.*Execute.*Verify')
  'DEEP-01 new-project' = ($c -match 'gsd-new-project.*det börjar här')
  'DEEP-02 architecture' = ($c -match 'ORCHESTRATOR' -and $c -match '18 agents')
  'DEEP-02 waves' = ($c -match 'Wave-baserad' -and $c -match 'Wave 1')
  'DEEP-03 directory' = ($c -match 'projektets minne' -and $c -match 'PROJECT\.md')
  'DEEP-03 artifacts' = ($c -match 'Artifacts.*faktiskt sparas' -and $c -match 'Success Criteria')
  'DEEP-04 memory' = ($c -match 'glömmer aldrig' -and $c -match 'resume-work')
  'DEEP-05 philosophy' = ($c -match 'Task completion' -and $c -match 'målet INTE uppnått')
  'DEEP-05 methodology' = ($c -match 'Goal-backward verification' -and $c -match 'SANT.*FINNAS.*KOPPLAT')
  'DEEP-06 flags' = ($c -match 'Composable flags' -and $c -match 'assistansnivån')
  'PRAC-08 lifecycle' = ($c -match 'Hela projektets livscykel' -and $c -match 'gsd-cleanup')
  'Structure intact' = ($c -match 'Börja enkelt' -and $c -match 'I din vardag' -and $c -match 'Tänk så här')
}
$checks.GetEnumerator() | ForEach-Object {
  Write-Output "$($_.Key): $(if ($_.Value) {'PASS'} else {'FAIL'})"
}
$allPass = ($checks.Values | Where-Object { -not $_ }).Count -eq 0
Write-Output "`nOverall: $(if ($allPass) {'ALL CHECKS PASS'} else {'SOME CHECKS FAILED'})"
```
</verification>

<success_criteria>
- 8 new slides in Section 5 of slides.md covering all assigned requirements
- ASCII art pipeline diagram shows complete 5-step lifecycle with artifact outputs
- Verification contrast slide ("task ≠ goal") is the most visually striking slide in the section
- Composable flags slide references Phase 2 without repeating terminal examples
- Persistent memory slide shows session break scenario (STATE.md → 3 days later → /gsd-resume-work)
- All existing slides (Sections 1-4, 6-8) remain unchanged
- Human has visually verified all 12 Phase 3 slides and approved
</success_criteria>

<output>
After completion, create `.planning/phases/03-core-payload-full-workflow/03-02-SUMMARY.md`
</output>
